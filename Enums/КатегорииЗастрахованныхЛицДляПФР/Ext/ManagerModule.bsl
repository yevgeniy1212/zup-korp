#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Возвращает код из 4 символов.
//
// Возвращаемое значение:
//   Строка
//
Функция КодКатегорииДляОтчета(Ссылка) Экспорт
	
	Код = ОбщегоНазначения.ИмяЗначенияПеречисления(Ссылка);
	Если Ссылка = КурильскиеОстрова Тогда
		Код = "ОСТ"	
	ИначеЕсли Ссылка = ВЖКурильскиеОстрова Тогда
		Код = "ВЖОС"	
	ИначеЕсли Ссылка = ВПКурильскиеОстрова Тогда
		Код = "ВПОС"	
	КонецЕсли;
	
	Возврат Код;
	
КонецФункции

#КонецОбласти

#КонецЕсли