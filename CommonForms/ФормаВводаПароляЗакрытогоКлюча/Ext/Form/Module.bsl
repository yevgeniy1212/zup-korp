#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.СерийныйНомер) И ЗначениеЗаполнено(Параметры.Поставщик) Тогда
		Поставщик 		= Параметры.Поставщик;
		СерийныйНомер 	= Параметры.СерийныйНомер;
	Иначе
		Отказ = Истина;
	КонецЕсли;
	
	Элементы.ДекорацияОшибка.Заголовок = Параметры.Заголовок;
	Элементы.ДекорацияОшибка.Видимость = ЗначениеЗаполнено(Параметры.Заголовок);
	
	Если ЗначениеЗаполнено(Параметры.Заголовок) Тогда
		КлючСохраненияПоложенияОкна = "Ошибка";
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Закрыть(ПарольДоступа);
	
КонецПроцедуры

#КонецОбласти