#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Функция ОснованиеВАрхиве(ОснованиеУвольнения) Экспорт
	
	Если Не ЗначениеЗаполнено(ОснованиеУвольнения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Менеджер = РегистрыСведений.ОснованияУвольненияВАрхиве.СоздатьМенеджерЗаписи();
	Менеджер.ОснованиеУвольнения = ОснованиеУвольнения;
	Менеджер.Прочитать();
	
	Возврат Менеджер.Выбран();
	
КонецФункции

Процедура ПоместитьОснованиеУвольненияВАрхив(ОснованиеУвольнения, ВАрхиве) Экспорт
	
	Менеджер = РегистрыСведений.ОснованияУвольненияВАрхиве.СоздатьМенеджерЗаписи();
	Менеджер.ОснованиеУвольнения = ОснованиеУвольнения;
	
	Если ВАрхиве Тогда
		Менеджер.Записать();
	Иначе
		Менеджер.Прочитать();
		Если Менеджер.Выбран() Тогда
			Менеджер.Удалить();
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
