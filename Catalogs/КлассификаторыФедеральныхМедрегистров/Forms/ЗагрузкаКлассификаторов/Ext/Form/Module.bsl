#Область ОписаниеПеременных

// Флаг подтверждения, используется при закрытии.
&НаКлиенте
Перем ПодтверждениеЗакрытияФормы;

&НаКлиенте
Перем ПараметрыОбработчикаОжидания;

&НаКлиенте
Перем ФормаДлительнойОперации;

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗначенияДляЗаполнения  = Новый Структура("Организация", "Организация");
	ЗарплатаКадры.ЗаполнитьПервоначальныеЗначенияВФорме(ЭтаФорма, ЗначенияДляЗаполнения);
	ПараметрыДлительнойОперации = Новый Структура("ИнтервалОжидания, Завершено, АдресРезультата, Идентификатор, Ошибка", 5);	
	ЗаполнитьТаблицуКлассификаторовДляЗагрузки();
	ЗаполнитьУИДОрганизацииИТокен(Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	//ПодтверждениеЗакрытияФормы = Истина;
    УстановитьИзмененияВИнтерфейсе("СтраницаИсточник");
		
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Если ПодтверждениеЗакрытияФормы<>Истина Тогда
		ТекстПредупреждения = НСтр("ru = 'Прервать загрузку классификаторов?'");
		Оповещение = Новый ОписаниеОповещения("ЗакрытиеФормыЗавершение", ЭтотОбъект);
		ОбщегоНазначенияКлиент.ПоказатьПодтверждениеЗакрытияФормы(Оповещение, Отказ, ЗавершениеРаботы, ТекстПредупреждения);
	КонецЕсли;
	
КонецПроцедуры

// Завершение диалога закрытия формы.
&НаКлиенте
Процедура ЗакрытиеФормыЗавершение(Знач РезультатВопроса, Знач ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ПодтверждениеЗакрытияФормы = Истина;
		Закрыть();
	Иначе 
		ПодтверждениеЗакрытияФормы = Неопределено;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
	Если ЗавершениеРаботы Тогда
		Возврат;
	КонецЕсли;	
	
	Если ПараметрыДлительнойОперации.Идентификатор <> Неопределено Тогда
		ОтменитьФоновоеЗадание(ПараметрыДлительнойОперации.Идентификатор);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ТокенПриИзменении(Элемент)
	ТокенПриИзмененииНаСервере(Организация, Токен);
КонецПроцедуры

&НаКлиенте
Процедура КлассификаторыДляЗагрузкиВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если Поле = Элементы.КлассификаторыДляЗагрузкиНаименованиеКлассификатора Тогда
		
		ТекущиеДанные = КлассификаторыДляЗагрузки.НайтиПоИдентификатору(ВыбраннаяСтрока);
		
		Если ТекущиеДанные <> Неопределено Тогда
			
			ТекущиеДанные.Пометка = Не ТекущиеДанные.Пометка;
			
		КонецЕсли
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НадписьОткрытьЖурналРегистрацииНажатие(Элемент)
	ОткрытьФорму("Обработка.ЖурналРегистрации.Форма", Новый Структура("Пользователь", ИмяПользователя()));
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура УстановитьВыделитьВсе(Команда)
	
	УстановитьПометкиКлассификаторов(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтменитьВыделитьВсе(Команда)
	
	УстановитьПометкиКлассификаторов(Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура Загрузить(Команда)
	
	ОчиститьСообщения();
	КоличествоЗагружаемых = КлассификаторыДляЗагрузки.НайтиСтроки( Новый Структура("Пометка", Истина)).Количество();
	Если КоличествоЗагружаемых = 0 Тогда
		СообщениеПользователю = НСтр("ru = 'Не выбрано ни одного классификатора'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(СообщениеПользователю, , "КлассификаторыДляЗагрузки");
		Возврат;
	КонецЕсли;
	
	ЗагрузитьНаКлиенте();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьНаКлиенте()

	УстановитьИзмененияВИнтерфейсе("ОжиданиеЗагрузки");

	Результат = РезультатВыполненияВДлительнойОперации();

	Если Результат.ЗаданиеВыполнено Тогда
		ПроведениеВДлительнойОперации = Ложь;
		УстановитьИзмененияВИнтерфейсе("УспешноеЗавершение");
	Иначе
		ИдентификаторЗадания = Результат.ИдентификаторЗадания;
		АдресХранилища		 = Результат.АдресХранилища;
		
		ДлительныеОперацииКлиент.ИнициализироватьПараметрыОбработчикаОжидания(ПараметрыОбработчикаОжидания);
		ПодключитьОбработчикОжидания("Подключаемый_ПроверитьВыполнениеЗадания", 1, Истина);
		ФормаДлительнойОперации = ДлительныеОперацииКлиент.ОткрытьФормуДлительнойОперации(ЭтаФорма, ИдентификаторЗадания);
	КонецЕсли;
	
КонецПроцедуры
	
&НаСервере
Функция РезультатВыполненияВДлительнойОперации()
	
	ПомеченныеКлассификаторыДляЗагрузки = КлассификаторыДляЗагрузки.Выгрузить(КлассификаторыДляЗагрузки.НайтиСтроки( Новый Структура("Пометка", Истина)));
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("КлассификаторыДляЗагрузки", ПомеченныеКлассификаторыДляЗагрузки);
	СтруктураПараметров.Вставить("Токен", Токен);
	СтруктураПараметров.Вставить("УИНОрганизации", УИНОрганизации);
	СтруктураПараметров.Вставить("Отказ", Ложь);
	
	СохранитьНастройкуЗагрузкиКлассификаторовМедРегистра("ЗагружаемыеКлассификаторыФедеральныхМедрегистров", 
			ПомеченныеКлассификаторыДляЗагрузки.ВыгрузитьКолонку("КодКлассификатора"));
	
	НаименованиеЗадания = НСтр("ru = 'Загрузка классификаторов федеральных медицинских регистров'");
	
	Результат = ДлительныеОперации.ЗапуститьВыполнениеВФоне(
		УникальныйИдентификатор,
		"Справочники.КлассификаторыФедеральныхМедрегистров.ДлительнаяОперацияЗагрузкиКлассификаторов",
		СтруктураПараметров,
		НаименованиеЗадания);
	
	АдресХранилища = Результат.АдресХранилища;
	
	Если Результат.ЗаданиеВыполнено Тогда
		ЗагрузкаПослеВыполненияДлительнойОперации();
		ЗагрузкаВДлительнойОперации = Ложь;
	Иначе 
		ЗагрузкаВДлительнойОперации = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура ЗагрузкаПослеВыполненияДлительнойОперации()
	// Обновим ссылки на справочники. Иначе впервые загруженные классификаторы задвоятся, 
	// если повторить загрузку в той же сессии работы мастера загрузки.
	ТаблицаКлассификаторов = РеквизитФормыВЗначение("КлассификаторыДляЗагрузки");
	Справочники.КлассификаторыФедеральныхМедрегистров.ЗаполнитьСсылкиНаСправочники(ТаблицаКлассификаторов);
	ЗначениеВРеквизитФормы(ТаблицаКлассификаторов, "КлассификаторыДляЗагрузки");
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПроверитьВыполнениеЗадания()
	
	ЗаданиеВыполненоВДлительнойОперации = Ложь;
	
	Попытка
		Если ФормаДлительнойОперации.Открыта() 
			И ФормаДлительнойОперации.ИдентификаторЗадания = ИдентификаторЗадания Тогда
			Если ЗаданиеВыполнено(ИдентификаторЗадания) Тогда
				Если ЗагрузкаВДлительнойОперации Тогда
					ЗагрузкаПослеВыполненияДлительнойОперации();
				КонецЕсли;
				ДлительныеОперацииКлиент.ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
				ЗаданиеВыполненоВДлительнойОперации = Истина;
			Иначе
				ДлительныеОперацииКлиент.ОбновитьПараметрыОбработчикаОжидания(ПараметрыОбработчикаОжидания);
				ПодключитьОбработчикОжидания(
					"Подключаемый_ПроверитьВыполнениеЗадания",
					ПараметрыОбработчикаОжидания.ТекущийИнтервал,
					Истина);
			КонецЕсли;
		ИначеЕсли Не ФормаДлительнойОперации.Открыта() И ЗагрузкаВДлительнойОперации Тогда
			ЗагрузкаВДлительнойОперации = Ложь;
		КонецЕсли;
	Исключение
		ДлительныеОперацииКлиент.ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		СообщенияПользователю = СообщенияФоновогоЗадания(ИдентификаторЗадания);
		Если СообщенияПользователю <> Неопределено Тогда
			Для Каждого  СообщениеФоновогоЗадания Из СообщенияПользователю Цикл
				СообщениеФоновогоЗадания.Сообщить();
			КонецЦикла;
		КонецЕсли;
		УстановитьИзмененияВИнтерфейсе("АварийноеЗавершение");
		ВызватьИсключение;
	КонецПопытки;
	
	Если ЗагрузкаВДлительнойОперации И ЗаданиеВыполненоВДлительнойОперации Тогда
		ЗагрузкаВДлительнойОперации = Ложь;
		УстановитьИзмененияВИнтерфейсе("УспешноеЗавершение");
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаданиеВыполнено(ИдентификаторЗадания)
	Возврат ДлительныеОперации.ЗаданиеВыполнено(ИдентификаторЗадания);
КонецФункции

&НаСервереБезКонтекста
Функция СообщенияФоновогоЗадания(ИдентификаторЗадания)
	
	СообщенияПользователю = Новый Массив;
	ФоновоеЗадание = ФоновыеЗадания.НайтиПоУникальномуИдентификатору(ИдентификаторЗадания);
	Если ФоновоеЗадание <> Неопределено Тогда
		СообщенияПользователю = ФоновоеЗадание.ПолучитьСообщенияПользователю();
	КонецЕсли;
	
	Возврат СообщенияПользователю;
	
КонецФункции

&НаКлиенте                                
Процедура Далее(Команда)    
	
	Если Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.СтраницаИсточник Тогда
			
		ПродолжениеНевозможно = Ложь;
		Если ПустаяСтрока(УИНОрганизации) Тогда
			СообщениеПользователю = НСтр("ru = 'Не указан УИН организации'");
			ОбщегоНазначенияКлиент.СообщитьПользователю(СообщениеПользователю, , "УИНОрганизации");
			ПродолжениеНевозможно = Истина;
		КонецЕсли;
		Если ПустаяСтрока(Токен) Тогда
			СообщениеПользователю = НСтр("ru = 'Не указан токен'");
			ОбщегоНазначенияКлиент.СообщитьПользователю(СообщениеПользователю, , "Токен");
			ПродолжениеНевозможно = Истина;
		КонецЕсли;
		Если ПродолжениеНевозможно Тогда
			Возврат;
		КонецЕсли;
		
		УстановитьИзмененияВИнтерфейсе("СтраницаВыбор");

	КонецЕсли;	

КонецПроцедуры

&НаКлиенте
Процедура Назад(Команда)
	
	ТекстОписанияЗагрузки = "";
	
	Если Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.СтраницаВыбор Тогда
		
		УстановитьИзмененияВИнтерфейсе("СтраницаИсточник");
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.ОжиданиеЗагрузки Тогда
		
		Оповещение = Новый ОписаниеОповещения("ПрерватьЗагрузкуБезЗакрытияФормыЗавершение", ЭтотОбъект);
		
		Текст = НСтр("ru = 'Прервать загрузку классификаторов?'");
		ПоказатьВопрос(Оповещение, Текст, РежимДиалогаВопрос.ДаНет);
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.АварийноеЗавершение Тогда
		
		УстановитьИзмененияВИнтерфейсе("СтраницаВыбор");
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.УспешноеЗавершение Тогда
		
		УстановитьИзмененияВИнтерфейсе("СтраницаВыбор");
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Процедура ТокенПриИзмененииНаСервере(Организация, Токен = Неопределено)
	
	Если Не ЗначениеЗаполнено(Организация) Или ПустаяСтрока(Токен) Тогда
		Возврат;
	КонецЕсли;
	
	НастройкиОрганизации = МедицинскиеРегистры.СоздатьПустыеНастройкиОрганизации();
	НастройкиОрганизации.Токен = Токен;
	
	МедицинскиеРегистры.СохранитьНастройкиОрганизации(Организация, НастройкиОрганизации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПрерватьЗагрузкуБезЗакрытияФормыЗавершение(Знач РезультатВопроса, Знач ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		УстановитьИзмененияВИнтерфейсе("СтраницаВыбор");
	Иначе 
		ПодтверждениеЗакрытияФормы = Неопределено;
	КонецЕсли;
	
КонецПроцедуры

 // В зависимости от текущей страницы устанавливает доступность тех или иных полей для пользователя.
 //
&НаКлиенте
Процедура УстановитьИзмененияВИнтерфейсе(ИмяТекущейСтраницы)
	
	Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы[ИмяТекущейСтраницы];
	
	ПодтверждениеЗакрытияФормы = Истина;
	
	Если Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.СтраницаИсточник Тогда
		
		Элементы.КоманднаяПанельСтраницы.ТекущаяСтраница = Элементы.КоманднаяПанельНачало;
		Элементы.КнопкаДалее.КнопкаПоУмолчанию = Истина;
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.СтраницаВыбор Тогда
		
		Элементы.КоманднаяПанельСтраницы.ТекущаяСтраница = Элементы.КоманднаяПанельЗагрузить;
		Элементы.Загрузить.КнопкаПоУмолчанию = Истина;
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.ОжиданиеЗагрузки Тогда
		
		ПодтверждениеЗакрытияФормы = Неопределено;
		
		Элементы.КоманднаяПанельСтраницы.ТекущаяСтраница = Элементы.КоманднаяПанельПроцесс;
		Элементы.КнопкаПрерватьПроцесс.КнопкаПоУмолчанию = Истина;		
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.АварийноеЗавершение Тогда
		
		Элементы.КоманднаяПанельСтраницы.ТекущаяСтраница = Элементы.КоманднаяПанельПроцесс;
		Элементы.КнопкаПрерватьПроцесс.КнопкаПоУмолчанию = Истина;		
		
	ИначеЕсли Элементы.ШагиЗагрузки.ТекущаяСтраница = Элементы.УспешноеЗавершение Тогда
		
		Элементы.КоманднаяПанельСтраницы.ТекущаяСтраница = Элементы.КоманднаяПанельЗавершение;
		Элементы.КнопкаГотово.КнопкаПоУмолчанию = Истина;		
		ТекущийЭлемент = Элементы.КнопкаГотово;
		
	КонецЕсли;
	
КонецПроцедуры

// Заполняет таблицу значений КлассификаторыДляЗагрузки по значениям таблицы Классификаторов.
// Выбирается код, наименование и сокращение типа объекта.
//
&НаСервере
Процедура ЗаполнитьТаблицуКлассификаторовДляЗагрузки()
	
	ЗагружаемыеКлассификаторы = ЗагрузитьНастройкуЗагрузкиКлассификаторовМедРегистра("ЗагружаемыеКлассификаторыФедеральныхМедрегистров");
	
	КлассификаторыДляЗагрузки.Очистить();
	
	КлассификаторыФедеральныхМедрегистров = Справочники.КлассификаторыФедеральныхМедрегистров.ПолучитьСправочникиФРМРИзМакета();

	Для Каждого КлассификаторМР Из КлассификаторыФедеральныхМедрегистров Цикл
		НоваяСтрока = КлассификаторыДляЗагрузки.Добавить();
		ЗаполнитьЗначениясвойств(НоваяСтрока,КлассификаторМР);
		Если ЗагружаемыеКлассификаторы <> Неопределено Тогда
			Если ЗагружаемыеКлассификаторы.Найти(КлассификаторМР.КодКлассификатора) <> Неопределено Тогда
				НоваяСтрока.Пометка = Истина;
			Иначе
				НоваяСтрока.Пометка = Ложь;
			КонецЕсли;
		Иначе
			НоваяСтрока.Пометка = Истина; // Если в первый раз то отмечаем всех.
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьУИДОрганизацииИТокен(Организация)
	
	ДополнительныеСведенияОрганизации = МедицинскиеРегистры.ПолучитьНастройкиОрганизации(Организация);
	УИНОрганизации = ДополнительныеСведенияОрганизации.ИдентификаторДляМедрегистров;
	Токен = ДополнительныеСведенияОрганизации.Токен;
	
КонецПроцедуры
	
// Получает значение из системного хранилища настроек ИБ.
//
&НаСервереБезКонтекста
Функция ЗагрузитьНастройкуЗагрузкиКлассификаторовМедРегистра(КлючНастроек)
	Возврат ОбщегоНазначения.ХранилищеОбщихНастроекЗагрузить("ПараметрыЗагрузкиКлассификаторовФедеральныхМедрегистров", КлючНастроек);
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьНастройкуЗагрузкиКлассификаторовМедРегистра(КлючНастроек, Настройки)
	ОбщегоНазначения.ХранилищеОбщихНастроекСохранить("ПараметрыЗагрузкиКлассификаторовФедеральныхМедрегистров", КлючНастроек, Настройки);
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОтменитьФоновоеЗадание(Знач Идентификатор)
	
	Если Идентификатор <> Неопределено Тогда
		Попытка
			ДлительныеОперации.ОтменитьВыполнениеЗадания(Идентификатор);
		Исключение
			// Действие не требуется, запись в журнал регистрации уже произведена.
		КонецПопытки
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьПометкиКлассификаторов(Знач Пометка)
	
	Для Каждого Элемент Из КлассификаторыДляЗагрузки Цикл
		Элемент.Пометка = Истина;
	КонецЦикла;
	
	// Устанавливаем пометки только для видимых строк.
	ЭлементТаблицы = Элементы.КлассификаторыДляЗагрузки;
	Для Каждого СтрокаКлассификатора Из КлассификаторыДляЗагрузки Цикл
		Если ЭлементТаблицы.ДанныеСтроки( СтрокаКлассификатора.ПолучитьИдентификатор() ) <> Неопределено Тогда
			СтрокаКлассификатора.Пометка = Пометка;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#Область Загрузка

&НаСервере
Функция СостояниеФоновогоЗадания()
	
	Результат = Новый Структура("Прогресс, Завершено, Ошибка");
	
	Результат.Ошибка = "";
	
	Если ПараметрыДлительнойОперации.Идентификатор = Неопределено Тогда
		
		Результат.Завершено = Истина;
		Результат.Прогресс  = Неопределено;
		Результат.Ошибка    = ПараметрыДлительнойОперации.Ошибка;
		
	Иначе
		
		Попытка
			
			Результат.Завершено = ДлительныеОперации.ЗаданиеВыполнено(ПараметрыДлительнойОперации.Идентификатор);
			Результат.Прогресс  = ДлительныеОперации.ПрочитатьПрогресс(ПараметрыДлительнойОперации.Идентификатор);
			
		Исключение
			
			Результат.Ошибка = ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
			
		КонецПопытки
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура Подключаемый_ОжиданиеДлительнойОперации()
	
	// Обновим статус
	Состояние = СостояниеФоновогоЗадания();
	
	Если Не ПустаяСтрока(Состояние.Ошибка) Тогда
		
		УстановитьИзмененияВИнтерфейсе("АварийноеЗавершение");
		Возврат;
		
	ИначеЕсли Состояние.Завершено Тогда
		
		ТекстОписанияЗагрузки = НСтр("ru = 'Классификаторы успешно загружены.'");
		УстановитьИзмененияВИнтерфейсе("УспешноеЗавершение");	
		
		Оповестить("ЗагруженыКлассификаторыФедеральныхМедрегистров", , ЭтотОбъект);
		ПодтверждениеЗакрытияФормы = Истина;
		
		Возврат;
		
	КонецЕсли;
	
	// Процесс продолжается
	Если ТипЗнч(Состояние.Прогресс) = Тип("Структура") Тогда
		
		ТекстСостоянияЗагрузки = Состояние.Прогресс.Текст;
		
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОжиданиеДлительнойОперации", ПараметрыДлительнойОперации.ИнтервалОжидания, Истина);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти
