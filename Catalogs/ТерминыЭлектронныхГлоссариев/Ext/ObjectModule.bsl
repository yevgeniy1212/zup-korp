#Если Сервер ИЛИ ТолстыйКлиентОбычноеПриложение ИЛИ ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если НЕ ЭтоГруппа Тогда

		Если Справочники.ТерминыЭлектронныхГлоссариев.ПредставлениеСодержитЗапрещенныеСимволы(Наименование) Тогда
			ПроверяемыеРеквизиты.Удалить(ПроверяемыеРеквизиты.Найти("Наименование"));
			ТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Представление термина не должно содержать символы %1 или быть пустой строкой'"), 
				Справочники.ТерминыЭлектронныхГлоссариев.ЗапрещенныеСимволыСтрокой());
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки,,"Наименование",,Отказ);
		КонецЕсли;
		
		Для каждого СтрокаТЧС Из Синонимы Цикл
			Если Справочники.ТерминыЭлектронныхГлоссариев.ПредставлениеСодержитЗапрещенныеСимволы(СтрокаТЧС.Представление) Тогда
				ТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Представление синонима не должно содержать символы %1 или быть пустой строкой'"), 
					Справочники.ТерминыЭлектронныхГлоссариев.ЗапрещенныеСимволыСтрокой());
				ОбщегоНазначения.СообщитьПользователю(ТекстОшибки,,,,Отказ);
				Возврат;
			КонецЕсли;			
		КонецЦикла;
		
	КонецЕсли;
		
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда 
		Возврат; 
	КонецЕсли;		
		
	РазработкаЭлектронныхКурсовСлужебный.ПроверитьВозможностьЗаписиЭлементаПередЗаписью(ЭтотОбъект, Отказ);
	РазработкаЭлектронныхКурсовСлужебный.УстановитьДатуИзмененияПередЗаписью(ЭтотОбъект, Отказ);
	
	Если НЕ ЭтоГруппа Тогда
		
		БукваДляПоиска = ВРег(БукваДляПоиска);
		
		Для каждого СтрокаТС Из Синонимы Цикл
			СтрокаТС.БукваДляПоиска = ВРег(СтрокаТС.БукваДляПоиска);
		КонецЦикла;
		
	КонецЕсли;
		
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	Если ОбменДанными.Загрузка Тогда 
		Возврат; 
	КонецЕсли;
	РазработкаЭлектронныхКурсовСлужебный.ПриЗаписиЭлементаЭлектронногоКурса(ЭтотОбъект);
КонецПроцедуры

Процедура ПриКопировании(ОбъектКопирования)
	ДополнительныеСвойства.Вставить("ОбъектКопирования", ОбъектКопирования);
КонецПроцедуры

#КонецОбласти

#КонецЕсли

