#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	
	Если Не Ссылка.Категория = Перечисления.КатегорииРасходовНаПерсонал.КоличествоСтавок Тогда
		Возврат;
	КонецЕсли;
	
	Если Не Категория = Перечисления.КатегорииРасходовНаПерсонал.КоличествоСтавок Тогда
		ВызватьИсключение НСтр("ru = 'Изменение категории предопределенного показателя запрещено.'");
	КонецЕсли;
	
	Если ПометкаУдаления Тогда
		ВызватьИсключение НСтр("ru = 'Пометка на удаление предопределенного показателя запрещена.'");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
