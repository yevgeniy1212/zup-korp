#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьПредставлениеПериодичности();
	
	МедицинскиеОсмотры.СохранитьПрежнююПериодичностьВредногоФактораВидаРабот(ЭтотОбъект);
	
	Если ВАрхиве Тогда
		МедицинскиеОсмотры.СохранитьПрежнийНаборСоответствующихВредныхФакторов(ЭтотОбъект);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	МедицинскиеОсмотры.ПроверитьИзменениеДанныхВредногоФактора(ЭтотОбъект);
	
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	ЗаполнитьПоДаннымЗаполнения(ДанныеЗаполнения);
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если Однократно = Истина Тогда
		РеквизитКратностьОсмотра = ПроверяемыеРеквизиты.Найти("КратностьОсмотра");
		Если РеквизитКратностьОсмотра <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитКратностьОсмотра);
		КонецЕсли;
		РеквизитПериодичностьОсмотра = ПроверяемыеРеквизиты.Найти("ПериодичностьОсмотра");
		Если РеквизитПериодичностьОсмотра <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитПериодичностьОсмотра);
		КонецЕсли;
		РеквизитЕдиницаПериодичностиОсмотра = ПроверяемыеРеквизиты.Найти("ЕдиницаПериодичностиОсмотра");
		Если РеквизитЕдиницаПериодичностиОсмотра <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитЕдиницаПериодичностиОсмотра);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьПредставлениеПериодичности() Экспорт
	
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	
	ПредставлениеПериодичностиОсмотра = МедицинскиеОсмотры.ПредставлениеПериодичности(
		КратностьОсмотра, ПериодичностьОсмотра, ЕдиницаПериодичностиОсмотра, Однократно);
	
КонецПроцедуры

Процедура ЗаполнитьПоДаннымЗаполнения(ДанныеЗаполнения)
	
	Если ТипЗнч(ДанныеЗаполнения) <> Тип("Структура") Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, ДанныеЗаполнения);
	
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли