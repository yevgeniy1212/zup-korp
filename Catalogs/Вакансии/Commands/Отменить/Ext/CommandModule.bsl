
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПубликацииВакансий = Неопределено;
	
	ОтменитьНабор(ПараметрКоманды, НачалоДня(ОбщегоНазначенияКлиент.ДатаСеанса()), ПубликацииВакансий);
	ОбщегоНазначенияКлиент.ОповеститьОбИзмененииОбъектов(ПараметрКоманды);
	
	НачатьСнятиеПубликаций(ПубликацииВакансий);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ОтменитьНабор(Вакансии, ДатаОтмены, ПубликацииВакансий)
	
	Справочники.Вакансии.ОтменитьНабор(Вакансии, ДатаОтмены);
	
	ПубликацииВакансий = РекрутинговыеСайты.СайтыСПубликациямиВакансий(Вакансии, , Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура НачатьСнятиеПубликаций(ПубликацииВакансий)
	
	Если ПубликацииВакансий = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ПубликацииВакансий.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ТекстВопроса = НСтр("ru = 'Снять активные публикации на сайтах?'");
	
	ДополнительныеПараметры = Новый Структура("ПубликацииВакансий");
	ДополнительныеПараметры.ПубликацииВакансий = ПубликацииВакансий;
	
	Оповещение = Новый ОписаниеОповещения("ЗавершитьСнятиеПубликаций", ЭтотОбъект, ДополнительныеПараметры);
	
	ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьСнятиеПубликаций(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	РекрутинговыеСайтыКлиент.СнятьПубликациюВакансий(ДополнительныеПараметры.ПубликацииВакансий);
	
КонецПроцедуры

#КонецОбласти
