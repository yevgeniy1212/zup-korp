
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если Параметры.Свойство("ПредоставлятьВычет") Тогда
		Заголовок = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='График платежей по виду вычета ""%1"" у физического лица ""%2""'"),Параметры.ВидВычетаИПН, Параметры.ФизическоеЛицо); 
		Если Параметры.ПредоставлятьВычет Тогда
			Элементы.ПрочиеВычеты.ТолькоПросмотр = Ложь;
			Элементы.ПрочиеВычетыКоманднаяПанель.Доступность = Истина;
		Иначе
			Элементы.ПрочиеВычеты.ТолькоПросмотр = Истина;
			Элементы.ПрочиеВычетыКоманднаяПанель.Доступность = Ложь;
		КонецЕсли;
	Иначе
		Заголовок = СтрЗаменить(НСтр("ru='График платежей по виду вычета ""%1""'"),"%1",Параметры.ВидВычетаИПН);
	КонецЕсли;
	
	ПолучитьПараметры();
	ПересчитатьНомераСтрок();
    РазмерМРП = ЗарплатаКадрыПовтИсп.МесячныйРасчетныйПоказатель(?(ЗначениеЗаполнено(МесяцЗавершения), МесяцЗавершения, Месяц));
	УправлениеФормой(ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, СтандартнаяОбработка)
	
	Оповещение = Новый ОписаниеОповещения("ВыбратьИЗакрыть", ЭтотОбъект);
	ОбщегоНазначенияКлиент.ПоказатьПодтверждениеЗакрытияФормы(Оповещение, Отказ, ЗавершениеРаботы);
	
КонецПроцедуры

&НаСервере
Процедура ОбработкаПроверкиЗаполненияНаСервере(Отказ, ПроверяемыеРеквизиты)
	
	Ошибки = Неопределено;
	
	Для каждого Строка Из ПрочиеВычеты Цикл
		
		ПроверитьСтрокуТЧ(
			Месяц,
			МесяцЗавершения,
			Строка.ДействуетС,
			Строка.ДействуетПо,
			Строка.НомерСтроки,
			Строка.Размер,
			Строка.ПолучитьИдентификатор(),
			Ошибки);
		
	КонецЦикла;
	
	ОбщегоНазначенияКлиентСервер.СообщитьОшибкиПользователю(Ошибки, Отказ);
	
	ВычетыИПНВПределахГода = ВидыВычетовСКонтролемПериода();
	
	Если НЕ Отказ И НЕ ВычетыИПНВПределахГода.Найти(ВидВычетаИПН) = Неопределено Тогда
		
		ПроверитьНазначениеВычетовВПределахГода(Отказ);
		
	КонецЕсли;
	
	Если Не Отказ Тогда
		ПроверитьПересечениеПериодов(Отказ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	ВыбратьИЗакрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Модифицированность = Ложь;
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыПрочиеВычеты

&НаКлиенте
Процедура ПрочиеВычетыДействуетПоПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.ПрочиеВычеты.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТекущиеДанные.ДействуетПо) Тогда
		ТекущиеДанные.ДействуетПо = КонецМесяца(ТекущиеДанные.ДействуетПо);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПрочиеВычетыДействуетСПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.ПрочиеВычеты.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТекущиеДанные.ДействуетС) Тогда
		ТекущиеДанные.ДействуетС = НачалоМесяца(ТекущиеДанные.ДействуетС);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПрочиеВычетыПослеУдаления(Элемент)
	ПересчитатьНомераСтрок();
	ОбновитьИтоги(ЭтаФорма);
КонецПроцедуры

&НаКлиенте
Процедура ПрочиеВычетыПриНачалеРедактирования(Элемент, НоваяСтрока, Копирование)
	
	ТекущиеДанные = Элементы.ПрочиеВычеты.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если НоваяСтрока Тогда
		
		ДатаНачала = Дата(1,1,1);
		ДатаОкончания = Дата (1,1,1);
		
		Если ПрочиеВычеты.Количество() = 1 Тогда
			ДатаНачала = Месяц;
			ДатаОкончания = КонецМесяца(ДатаНачала);
		Иначе
			ДатаОкончанияПредыдущего = ПрочиеВычеты[ПрочиеВычеты.Количество() - 2].ДействуетПо;
			Если ЗначениеЗаполнено(ДатаОкончанияПредыдущего) Тогда
				ДатаНачала = НачалоДня(КонецДня(ДатаОкончанияПредыдущего) + 1);
				ДатаОкончания = КонецМесяца(ДатаНачала);
			КонецЕсли;
		КонецЕсли;
		
		ТекущиеДанные.ДействуетС = ДатаНачала;
		ТекущиеДанные.ДействуетПо = ДатаОкончания;
		ТекущиеДанные.НомерСтроки = ПрочиеВычеты.Количество();
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПрочиеВычетыПриОкончанииРедактирования(Элемент, НоваяСтрока, ОтменаРедактирования)
	
	Если НЕ ОтменаРедактирования Тогда
		ОбновитьИтоги(ЭтаФорма);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ПолучитьПараметры()
	
	Параметры.Свойство("ВидВычетаИПН",		ВидВычетаИПН);
	Параметры.Свойство("ФизическоеЛицо",	ФизическоеЛицо);
	Параметры.Свойство("Месяц",				Месяц);
	Параметры.Свойство("МесяцЗавершения",	МесяцЗавершения);
	ПрочиеВычеты.Загрузить(ПолучитьИзВременногоХранилища(Параметры.АдресВХранилищеПрочиеВычеты));
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьИЗакрыть(Результат = Неопределено, ДополнительныеПараметры = Неопределено) Экспорт
	
	Если ПроверитьЗаполнение() Тогда
		
		РезультатыРедактирования = Новый Структура;
		РезультатыРедактирования.Вставить("Модифицированность", Модифицированность);
		РезультатыРедактирования.Вставить("ВидВычетаИПН",       ВидВычетаИПН);
		РезультатыРедактирования.Вставить("ФизическоеЛицо",		ФизическоеЛицо);
		РезультатыРедактирования.Вставить("АдресВХранилищеПрочиеВычеты", АдресВХранилищеПрочиеВычеты());
		
		Модифицированность = Ложь;
		Закрыть(РезультатыРедактирования)
		
	КонецЕсли
	
КонецПроцедуры

&НаСервере
Функция АдресВХранилищеПрочиеВычеты()
	
	ТЗПрочиеВычеты = ПрочиеВычеты.Выгрузить(,"ФизическоеЛицо,ВидВычетаИПН,Размер,ДействуетПо,ДействуетС");
	ТЗПрочиеВычеты.ЗаполнитьЗначения(ВидВычетаИПН, "ВидВычетаИПН");
	ТЗПрочиеВычеты.ЗаполнитьЗначения(ФизическоеЛицо, "ФизическоеЛицо");
	
	Возврат ПоместитьВоВременноеХранилище(ТЗПрочиеВычеты, УникальныйИдентификатор);
	
КонецФункции

&НаСервере
Процедура ПересчитатьНомераСтрок()
	
	НомерСтроки = 1;
	Для каждого Строка Из ПрочиеВычеты Цикл
		Строка.НомерСтроки = НомерСтроки;
		НомерСтроки = НомерСтроки + 1;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ПроверитьСтрокуТЧ(Месяц,МесяцЗавершения,ДатаНачала,ДатаОкончания,НомерСтроки,Размер,Идентификатор,Ошибки)
	
	Если Не ЗначениеЗаполнено(ДатаНачала) Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].ДействуетС",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета период начала действия не может быть пустым.'"),НомерСтроки),
			"",
			НомерСтроки - 1);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ДатаОкончания) Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].ДействуетПо",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета период окончания действия вычета не может быть пустым.'"),НомерСтроки),
			"",
			НомерСтроки - 1);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДатаНачала) И ЗначениеЗаполнено(ДатаОкончания) И ДатаНачала > ДатаОкончания Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].ДействуетС",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета период начала действия вычета превышает дату окончания.'"),НомерСтроки),
			"",
			НомерСтроки - 1);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Месяц) И ДатаНачала < Месяц Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].ДействуетС",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета период начала действия вычета меньше даты начала действия заявления'"),НомерСтроки),
			"",
			НомерСтроки - 1);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(МесяцЗавершения) И ДатаОкончания > МесяцЗавершения Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].ДействуетПо",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета период окончания действия вычета превышает дату окончания действия заявления.'"),НомерСтроки),
			"",
			НомерСтроки - 1);
		КонецЕсли;
		
	Если Не ЗначениеЗаполнено(Размер) Тогда
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки,
			"ПрочиеВычеты[%1].Размер",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='В строке %1 графика вычета не заполнен размер вычета.'"),НомерСтроки),
			"",
			НомерСтроки - 1);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ВидыВычетовСКонтролемПериода()
	
	ВидыВычетовИПН = Новый Массив;
	
	ВидыВычетовИПН.Добавить(Справочники.ВидыВычетовИПН.ПредварительнаяСуммаНалоговыхВычетов);
	
	Возврат	ВидыВычетовИПН;
	
КонецФункции

&НаСервере
Процедура ПроверитьПересечениеПериодов(Отказ)
	Ошибки = Неопределено;
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("ПрочиеВычеты",ПрочиеВычеты.Выгрузить());
	Запрос.Текст = "ВЫБРАТЬ
	               |	Таблица.ДействуетС,
	               |	Таблица.ДействуетПо,
	               |	Таблица.НомерСтроки
	               |ПОМЕСТИТЬ ВТ_Таблица
	               |ИЗ
	               |	&ПрочиеВычеты КАК Таблица
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ
	               |	Периоды.НомерСтроки,
	               |	ПериодыСравнения.НомерСтроки КАК НомерСтрокиПересечения
	               |ИЗ
	               |	ВТ_Таблица КАК Периоды
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Таблица КАК ПериодыСравнения
	               |		ПО Периоды.ДействуетС <= ПериодыСравнения.ДействуетПо
	               |			И Периоды.ДействуетПо >= ПериодыСравнения.ДействуетС
	               |			И Периоды.НомерСтроки <> ПериодыСравнения.НомерСтроки";
	
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		ТекстСообщения = НСтр("ru='Период действия строки %1 таблицы пересекается с периодом строки: %2.'");
		ТекстСообщения = СтрЗаменить(ТекстСообщения,"%1", Выборка.НомерСтроки);
		ТекстСообщения = СтрЗаменить(ТекстСообщения,"%2", Выборка.НомерСтрокиПересечения);
		
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(
			Ошибки, "ПрочиеВычеты[%1].ДействуетС", ТекстСообщения,"",Выборка.НомерСтроки - 1);
		
	КонецЦикла;
	
	ОбщегоНазначенияКлиентСервер.СообщитьОшибкиПользователю(Ошибки, Отказ);
	
КонецПроцедуры

&НаСервере
Процедура ПроверитьНазначениеВычетовВПределахГода(Отказ)
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("ПрочиеВычеты", ПрочиеВычеты.Выгрузить());
	Запрос.Текст = "ВЫБРАТЬ
	               |	Таблица.ДействуетС,
	               |	Таблица.ДействуетПо,
	               |	Таблица.НомерСтроки
	               |ПОМЕСТИТЬ ВТ_Таблица
	               |ИЗ
	               |	&ПрочиеВычеты КАК Таблица
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ
	               |	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ГОД(Периоды.ДействуетС)) КАК КоличествоЛет,
	               |	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ГОД(Периоды.ДействуетПо)) КАК КоличествоЛетПо,
	               |	МАКСИМУМ(ВЫБОР КОГДА ГОД(Периоды.ДействуетС) <> ГОД(Периоды.ДействуетПо) ТОГДА ИСТИНА ИНАЧЕ ЛОЖЬ КОНЕЦ) КАК СтрокаБольшеГода
	               |ИЗ
	               |	ВТ_Таблица КАК Периоды
				   |";
	
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		Если Выборка.КоличествоЛет > 1 ИЛИ Выборка.КоличествоЛетПо > 1 ИЛИ Выборка.СтрокаБольшеГода Тогда
			
			ТекстСообщения = НСтр("ru = ' График платежей вводится в течение календарного года, если требуется ввести график платежей на разные года, то необходимо сформировать два разных документа на каждый год в отдельности'");
			
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,,, Отказ);
				
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УправлениеФормой(Форма)

	Элементы = Форма.Элементы;
	// итоги отражаем для контроля пользователя общего предела по предварительной сумме вычетов
	Элементы.ГруппаИтоги.Видимость = (Форма.ВидВычетаИПН = ПредопределенноеЗначение("Справочник.ВидыВычетовИПН.ПредварительнаяСуммаНалоговыхВычетов"));
	ЗаявлениеСформированоЗаОдинГод = (Год(Форма.Месяц) = Год(Форма.МесяцЗавершения));
	Элементы.ИтогиВсегоВМРП.Видимость = ЗаявлениеСформированоЗаОдинГод;
    Элементы.ДекорацияБезМРП.Видимость = НЕ ЗаявлениеСформированоЗаОдинГод;
	
	ОбновитьИтоги(Форма);

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ОбновитьИтоги(Форма)
	
	ПрочиеВычеты = Форма.ПрочиеВычеты;
	
	ЕстьСвернутыеПериоды = Ложь;
	
	Для Каждого СтрокаВычета Из ПрочиеВычеты Цикл
		
		Если НЕ Месяц(СтрокаВычета.ДействуетС) = Месяц(СтрокаВычета.ДействуетПо) Тогда 
			
			ЕстьСвернутыеПериоды = Истина;
			
			Прервать;
			
		КонецЕсли;
			
	КонецЦикла;
	
	Если ЕстьСвернутыеПериоды Тогда
		
		ИтогиВсего = 0;
		
		Для Каждого СтрокаВычета Из ПрочиеВычеты Цикл
			
			Если НЕ Месяц(СтрокаВычета.ДействуетС) = Месяц(СтрокаВычета.ДействуетПо) Тогда
				
				КоличествоМесяцев = Месяц(СтрокаВычета.ДействуетПо) - Месяц(СтрокаВычета.ДействуетС) + 1;
				
				ИтогиВсего = ИтогиВсего + СтрокаВычета.Размер * КоличествоМесяцев;

			Иначе
				
				ИтогиВсего = ИтогиВсего + СтрокаВычета.Размер;
				
			КонецЕсли;
		
		КонецЦикла;
		
	Иначе
		
		ИтогиВсего = ПрочиеВычеты.Итог("Размер");

	КонецЕсли;
	
	Форма.ИтогиВсего = 	ИтогиВсего;
	
	Если Форма.РазмерМРП > 0 Тогда
		
		Форма.ИтогиВсегоВМРП = Окр(ИтогиВсего / Форма.РазмерМРП, 2);
		
	КонецЕсли;

КонецПроцедуры

#КонецОбласти
