#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#КонецОбласти

#Область СлужебныеПроцедурыИФункции
Функция ДанноеУведомлениеДоступноДляОрганизации() Экспорт 
	Возврат Истина;
КонецФункции

Функция ДанноеУведомлениеДоступноДляИП() Экспорт 
	Возврат Истина;
КонецФункции

Функция ПолучитьОсновнуюФорму() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьФормуПоУмолчанию() Экспорт 
	Возврат "";
КонецФункции

Функция ПолучитьТаблицуФорм() Экспорт 
	Результат = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюТаблицуФормУведомления();
	
	Стр = Результат.Добавить();
	Стр.ИмяФормы = "Форма2021_1";
	Стр.ОписаниеФормы = "Приказ ФНС России от 24.05.2021 № ЕД-7-15/513@";
	Стр.ДатаНачала = '20210701';
	Стр.ДатаКонца = '20991231';

	Возврат Результат;
КонецФункции

Функция ЭлектронноеПредставление(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
	Если ИмяФормы = "Форма2020_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2020_1(Объект, УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда
		Возврат ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор);
	КонецЕсли;
	Возврат Неопределено;
КонецФункции

Функция ПроверитьДокумент(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт
КонецФункции

Функция СформироватьСписокЛистов(Объект) Экспорт
	Если Объект.ИмяФормы = "Форма2020_1" Тогда 
		Возврат СформироватьСписокЛистовФорма2020_1(Объект);
	ИначеЕсли Объект.ИмяФормы = "Форма2021_1" Тогда 
		Возврат СформироватьСписокЛистовФорма2021_1(Объект);
	КонецЕсли;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу(Объект, ИмяФормы, УникальныйИдентификатор) Экспорт 
	Если ИмяФормы = "Форма2020_1" Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2020_1(УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	ИначеЕсли ИмяФормы = "Форма2021_1" Тогда 
		Возврат ПроверитьДокументСВыводомВТаблицу_Форма2021_1(УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект), УникальныйИдентификатор);
	КонецЕсли;
КонецФункции

Процедура ВывестиОбласть(Объект, Листы, Область, ПечатнаяФорма, Колонки, НомерЛиста)
	Если Не ПечатнаяФорма.ПроверитьВывод(Область) Тогда 
		ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомерЛиста);
		ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
		ПечатнаяФорма.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		ПечатнаяФорма.Вывести(Колонки);
	КонецЕсли;
	
	ПечатнаяФорма.Вывести(Область);
КонецПроцедуры

Функция СформироватьСписокЛистовФорма2020_1(Объект)
	НомерЛиста = 0;
	ОТД = Новый ОписаниеТипов("Дата");
	Листы = Новый СписокЗначений;
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	ПечатнаяФорма.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	СтруктураПараметров = Объект.Ссылка.ДанныеУведомления.Получить().СтруктураРеквизитов;
	
	МакетПФ = Отчеты[Объект.ИмяОтчета].ПолучитьМакет("Печать_MXL_Форма2020_1");
	ШапкаИнформация = МакетПФ.ПолучитьОбласть("ШапкаИнформация");
	Шапка = МакетПФ.ПолучитьОбласть("Шапка");
	Строка = МакетПФ.ПолучитьОбласть("Строка");
	Колонки = МакетПФ.ПолучитьОбласть("Колонки");
	ИтогиПоКоду = МакетПФ.ПолучитьОбласть("ИтогиПоКоду");
	
	Шапка.Параметры.НаимОрг = ?(РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Объект.Организация),
								СтруктураПараметров._НаимОрг,
								СтруктураПараметров._Фамилия + " " + СтруктураПараметров._Имя + " " + СтруктураПараметров._Отчество);
	Шапка.Параметры.КвОтчета = Месяц(КонецКвартала(СтруктураПараметров._Период)) / 3;
	Шапка.Параметры.ГодОтчета = Формат(Год(СтруктураПараметров._Период), "ЧГ=");
	
	_РеестрДокПОбНЛ = СтруктураПараметров._РеестрДокПОбНЛ.Скопировать();
	СтруктураОтбора = Новый Структура("КодОпер");
	СтруктураОтбораВДереве = Новый Структура("УИД");
	_РеестрДокПОбНЛ.Свернуть("КодОпер");
	_РеестрДокПОбНЛ.Сортировать("КодОпер");
	
	ПечатнаяФорма.Вывести(ШапкаИнформация);
	ПечатнаяФорма.Вывести(Шапка);
	
	Для Каждого СтрКодОпер Из _РеестрДокПОбНЛ Цикл
		Для Инд = 0 По Строка.Параметры.Количество() - 1 Цикл 
			Строка.Параметры[Инд] = Неопределено;
		КонецЦикла;
		СтруктураОтбора.КодОпер = СтрКодОпер.КодОпер;
		НайденныеСтроки = СтруктураПараметров._РеестрДокПОбНЛ.НайтиСтроки(СтруктураОтбора);
		Строка.Параметры.КодОперации = СтрКодОпер.КодОпер;
		
		ИтогПоВидам = 0;
		ИтогПоОперациям = 0;
		Для Каждого Стр2 Из НайденныеСтроки Цикл
			ИтогПоВидам = ИтогПоВидам + Стр2.СумНеоблОпер;
			Строка.Параметры.ВидОперации = Стр2.ВидОпер;
			Строка.Параметры.СуммаПоВиду = Стр2.СумНеоблОпер;
			СтруктураОтбораВДереве.УИД = Стр2.УИД;
			СтрокиВДереве = СтруктураПараметров._ПредТипДог.Строки.НайтиСтроки(СтруктураОтбораВДереве, Ложь);
			Если СтрокиВДереве.Количество() = 0 Тогда 
				ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
				ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 4, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
					ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
				Продолжить;
			КонецЕсли;
			
			Для Каждого стрДерево1 Из СтрокиВДереве Цикл 
				Для Каждого стрДерево2 Из стрДерево1.Строки Цикл
					Строка.Параметры.Контрагент = ?(ЗначениеЗаполнено(стрДерево2.НаимОрг),
						стрДерево2.НаимОрг,
						стрДерево2.Фамилия + " " + стрДерево2.Имя + " " + стрДерево2.Отчество);
					Строка.Параметры.КонтрагентИНН = ?(ЗначениеЗаполнено(стрДерево2.ИННЮЛ), стрДерево2.ИННЮЛ, стрДерево2.ИННФЛ);
					Строка.Параметры.КонтрагентКПП = стрДерево2.КПП;
					Строка.Параметры.СуммаОперации = стрДерево2.СумОпер;
					
					Если стрДерево2.Строки.Количество() = 0 Тогда 
						ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
					КонецЕсли;
					ИтогПоОперациям = ИтогПоОперациям + стрДерево2.СумОпер;
					
					Для Каждого стрДерево3 Из стрДерево2.Строки Цикл
						Строка.Параметры.ТипДокумента = стрДерево3.ТипДок;
						Строка.Параметры.НомерДокумента = стрДерево3.НомДок;
						Строка.Параметры.ДатаДокумента = Формат(стрДерево3.ДатаДок, "ДФ=dd.MM.yyyy; ДП=' '");
						ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
						
						Строка.Параметры.КодОперации = "";
						Строка.Параметры.ВидОперации = "";
						Строка.Параметры.СуммаПоВиду = "";
						Строка.Параметры.Контрагент = "";
						Строка.Параметры.КонтрагентИНН = "";
						Строка.Параметры.КонтрагентКПП = "";
						Строка.Параметры.СуммаОперации = "";
					КонецЦикла;
					
					Строка.Параметры.КодОперации = "";
					Строка.Параметры.ВидОперации = "";
					Строка.Параметры.СуммаПоВиду = "";
					Строка.Параметры.Контрагент = "";
					Строка.Параметры.КонтрагентИНН = "";
					Строка.Параметры.КонтрагентКПП = "";
					Строка.Параметры.СуммаОперации = "";
					ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 7, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
						ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
				КонецЦикла;
			КонецЦикла;
			
			Строка.Параметры.КодОперации = "";
			ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 4, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
				ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
		КонецЦикла;
		
		ИтогиПоКоду.Параметры.ИтогПоВидам = ИтогПоВидам;
		ИтогиПоКоду.Параметры.ИтогПоОперациям = ИтогПоОперациям;
		ВывестиОбласть(Объект, Листы, ИтогиПоКоду, ПечатнаяФорма, Колонки, НомерЛиста);
	КонецЦикла;
	
	ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомерЛиста);
	Возврат Листы;
КонецФункции

Функция СформироватьСписокЛистовФорма2021_1(Объект)
	НомерЛиста = 0;
	ОТД = Новый ОписаниеТипов("Дата");
	Листы = Новый СписокЗначений;
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
	ПечатнаяФорма.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	СтруктураПараметров = Объект.Ссылка.ДанныеУведомления.Получить().СтруктураРеквизитов;
	
	МакетПФ = Отчеты[Объект.ИмяОтчета].ПолучитьМакет("Печать_MXL_Форма2021_1");
	ШапкаИнформация = МакетПФ.ПолучитьОбласть("ШапкаИнформация");
	Шапка = МакетПФ.ПолучитьОбласть("Шапка");
	Строка = МакетПФ.ПолучитьОбласть("Строка");
	Колонки = МакетПФ.ПолучитьОбласть("Колонки");
	ИтогиПоКоду = МакетПФ.ПолучитьОбласть("ИтогиПоКоду");
	Сноска = МакетПФ.ПолучитьОбласть("Сноска");
	
	Шапка.Параметры.НаимОрг = ?(РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Объект.Организация),
								СтруктураПараметров._НаимОрг,
								СтруктураПараметров._Фамилия + " " + СтруктураПараметров._Имя + " " + СтруктураПараметров._Отчество);
	
	Шапка.Параметры._ОтчетГод = СтруктураПараметров._ОтчетГод;
	Шапка.Параметры._Период = СтруктураПараметров._Период;
	Шапка.Параметры._КПП = СтруктураПараметров._КПП;
	Шапка.Параметры._ИмяФайлТреб = СтруктураПараметров._ИмяФайлТреб;
	Шапка.Параметры._ФормРеорг = СтруктураПараметров._ФормРеорг;
	Если ЗначениеЗаполнено(СтруктураПараметров._ИННЮЛРеорг) Или ЗначениеЗаполнено(СтруктураПараметров._КППРеорг) Тогда 
		Шапка.Параметры._ИННКППРеорг = СтруктураПараметров._ИННЮЛРеорг + "/" + СтруктураПараметров._КППРеорг;
	КонецЕсли;
	Шапка.Параметры._ИНН = ?(ЗначениеЗаполнено(СтруктураПараметров._ИННЮЛ), СтруктураПараметров._ИННЮЛ, СтруктураПараметров._ИННФЛ);
	Шапка.Параметры._НомКорр = Формат(СтруктураПараметров._НомКорр, "ЧН=0");
	
	_РеестрДокПОбНЛ = СтруктураПараметров._РеестрДокПОбНЛ.Скопировать();
	СтруктураОтбора = Новый Структура("КодОпер");
	СтруктураОтбораВДереве = Новый Структура("УИД");
	_РеестрДокПОбНЛ.Свернуть("КодОпер");
	_РеестрДокПОбНЛ.Сортировать("КодОпер");
	
	ПечатнаяФорма.Вывести(ШапкаИнформация);
	ПечатнаяФорма.Вывести(Шапка);
	
	Для Каждого СтрКодОпер Из _РеестрДокПОбНЛ Цикл
		Для Инд = 0 По Строка.Параметры.Количество() - 1 Цикл 
			Строка.Параметры[Инд] = Неопределено;
		КонецЦикла;
		СтруктураОтбора.КодОпер = СтрКодОпер.КодОпер;
		НайденныеСтроки = СтруктураПараметров._РеестрДокПОбНЛ.НайтиСтроки(СтруктураОтбора);
		Строка.Параметры.КодОперации = СтрКодОпер.КодОпер;
		
		ИтогПоВидам = 0;
		ИтогПоОперациям = 0;
		Для Каждого Стр2 Из НайденныеСтроки Цикл
			ИтогПоВидам = ИтогПоВидам + Стр2.СумНеоблОпер;
			Строка.Параметры.ВидОперации = Стр2.ВидОпер;
			Строка.Параметры.СуммаПоВиду = Стр2.СумНеоблОпер;
			СтруктураОтбораВДереве.УИД = Стр2.УИД;
			СтрокиВДереве = СтруктураПараметров._ПредТипДог.Строки.НайтиСтроки(СтруктураОтбораВДереве, Ложь);
			Если СтрокиВДереве.Количество() = 0 Тогда 
				//ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
				ПечатнаяФорма.Вывести(Строка);
				ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 4, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
					ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
				Продолжить;
			КонецЕсли;
			
			ВсеТиповыеДоговоры = Новый Соответствие;
			Для Каждого стрДерево1 Из СтрокиВДереве Цикл
				Если ЗначениеЗаполнено(стрДерево1.ОписаниеКА)
					И "<типовой договор не представлен>" <> стрДерево1.ОписаниеКА Тогда 
					ВсеТиповыеДоговоры.Вставить(стрДерево1.ОписаниеКА, Истина);
				КонецЕсли;
			КонецЦикла;
			
			Для Каждого стрДерево1 Из СтрокиВДереве Цикл
				Если Истина = ВсеТиповыеДоговоры[стрДерево1.ОписаниеКА] Тогда 
					Строка.Параметры.Контрагент = "По типовому договору " + стрДерево1.ОписаниеКА;
					Строка.Параметры.КонтрагентИНН = "-";
					Строка.Параметры.КонтрагентКПП = "-";
					Строка.Параметры.СуммаОперации = 0;
					Для Каждого стрДерево2 Из стрДерево1.Строки Цикл
						Строка.Параметры.СуммаОперации = Строка.Параметры.СуммаОперации + стрДерево2.СумОпер;
					КонецЦикла;
					Для Каждого стрДерево2 Из стрДерево1.Строки Цикл
						ИтогПоОперациям = ИтогПоОперациям + стрДерево2.СумОпер;
						
						Для Каждого стрДерево3 Из стрДерево2.Строки Цикл
							Строка.Параметры.ТипДокумента = стрДерево3.ТипДок;
							Строка.Параметры.НомерДокумента = стрДерево3.НомДок;
							Строка.Параметры.ДатаДокумента = Формат(стрДерево3.ДатаДок, "ДФ=dd.MM.yyyy; ДП=' '");
							//ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
							ПечатнаяФорма.Вывести(Строка);
							
							Строка.Параметры.КодОперации = "";
							Строка.Параметры.ВидОперации = "";
							Строка.Параметры.СуммаПоВиду = "";
							Строка.Параметры.Контрагент = "";
							Строка.Параметры.КонтрагентИНН = "";
							Строка.Параметры.КонтрагентКПП = "";
							Строка.Параметры.СуммаОперации = "";
						КонецЦикла;
						
						Строка.Параметры.КодОперации = "";
						Строка.Параметры.ВидОперации = "";
						Строка.Параметры.СуммаПоВиду = "";
						Строка.Параметры.Контрагент = "";
						Строка.Параметры.КонтрагентИНН = "";
						Строка.Параметры.КонтрагентКПП = "";
						Строка.Параметры.СуммаОперации = "";
					КонецЦикла;
					ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 7, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
						ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
				Иначе
					Для Каждого стрДерево2 Из стрДерево1.Строки Цикл
						Строка.Параметры.Контрагент = ?(ЗначениеЗаполнено(стрДерево2.НаимОрг),
							стрДерево2.НаимОрг,
							стрДерево2.Фамилия + " " + стрДерево2.Имя + " " + стрДерево2.Отчество);
						Строка.Параметры.КонтрагентИНН = ?(ЗначениеЗаполнено(стрДерево2.ИННЮЛ), стрДерево2.ИННЮЛ, стрДерево2.ИННФЛ);
						Строка.Параметры.КонтрагентКПП = стрДерево2.КПП;
						Строка.Параметры.СуммаОперации = стрДерево2.СумОпер;
						
						Если стрДерево2.Строки.Количество() = 0 Тогда 
							//ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
							ПечатнаяФорма.Вывести(Строка);
						КонецЕсли;
						ИтогПоОперациям = ИтогПоОперациям + стрДерево2.СумОпер;
						
						Для Каждого стрДерево3 Из стрДерево2.Строки Цикл
							Строка.Параметры.ТипДокумента = стрДерево3.ТипДок;
							Строка.Параметры.НомерДокумента = стрДерево3.НомДок;
							Строка.Параметры.ДатаДокумента = Формат(стрДерево3.ДатаДок, "ДФ=dd.MM.yyyy; ДП=' '");
							//ВывестиОбласть(Объект, Листы, Строка, ПечатнаяФорма, Колонки, НомерЛиста);
							ПечатнаяФорма.Вывести(Строка);
							
							Строка.Параметры.КодОперации = "";
							Строка.Параметры.ВидОперации = "";
							Строка.Параметры.СуммаПоВиду = "";
							Строка.Параметры.Контрагент = "";
							Строка.Параметры.КонтрагентИНН = "";
							Строка.Параметры.КонтрагентКПП = "";
							Строка.Параметры.СуммаОперации = "";
						КонецЦикла;
						
						Строка.Параметры.КодОперации = "";
						Строка.Параметры.ВидОперации = "";
						Строка.Параметры.СуммаПоВиду = "";
						Строка.Параметры.Контрагент = "";
						Строка.Параметры.КонтрагентИНН = "";
						Строка.Параметры.КонтрагентКПП = "";
						Строка.Параметры.СуммаОперации = "";
						ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 7, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
							ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
					КонецЦикла;
				КонецЕсли;
			КонецЦикла;
			
			Строка.Параметры.КодОперации = "";
			ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 4, ПечатнаяФорма.ВысотаТаблицы, 15).ГраницаСнизу = 
				ПечатнаяФорма.Область(ПечатнаяФорма.ВысотаТаблицы, 2, ПечатнаяФорма.ВысотаТаблицы, 2).ГраницаСлева;
		КонецЦикла;
		
		ИтогиПоКоду.Параметры.ИтогПоВидам = ИтогПоВидам;
		ИтогиПоКоду.Параметры.ИтогПоОперациям = ИтогПоОперациям;
		//ВывестиОбласть(Объект, Листы, ИтогиПоКоду, ПечатнаяФорма, Колонки, НомерЛиста);
		ПечатнаяФорма.Вывести(ИтогиПоКоду);
		ПечатнаяФорма.Вывести(Сноска);
	КонецЦикла;
	
	ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомерЛиста);
	Возврат Листы;
КонецФункции

Процедура ПоложитьПФВСписокЛистов(Объект, Листы, ПечатнаяФорма, НомерЛиста) Экспорт 
	НомерЛиста = НомерЛиста + 1;
	Лист = Новый Массив;
	Лист.Добавить(ПоместитьВоВременноеХранилище(ПечатнаяФорма));
	Лист.Добавить(Новый УникальныйИдентификатор);
	Лист.Добавить(Метаданные.Отчеты[Объект.ИмяОтчета].Синоним);
	Листы.Добавить(Лист, Метаданные.Отчеты[Объект.ИмяОтчета].Синоним + ", лист " + Формат(НомерЛиста, "ЧГ="));
	
	ПечатнаяФорма = УведомлениеОСпецрежимахНалогообложения.НовыйПустойЛист();
КонецПроцедуры

Функция ИдентификаторФайлаЭлектронногоПредставления_Форма2020_1(СведенияОтправки)
	Префикс = "KO_RROBNL";
	Возврат Документы.УведомлениеОСпецрежимахНалогообложения.ИдентификаторФайлаЭлектронногоПредставления(Префикс, СведенияОтправки);
КонецФункции

Функция ИдентификаторФайлаЭлектронногоПредставления_Форма2021_1(СведенияОтправки)
	Префикс = "KO_RROBNL";
	Возврат Документы.УведомлениеОСпецрежимахНалогообложения.ИдентификаторФайлаЭлектронногоПредставления(Префикс, СведенияОтправки);
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2020_1(Данные, УникальныйИдентификатор)
	ТаблицаОшибок = Новый СписокЗначений;
	ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
		"Устаревшая редакция формы", "ОсновныеСведения", "ДатаПодписи"));
	Возврат ТаблицаОшибок;
КонецФункции

Функция ПроверитьДокументСВыводомВТаблицу_Форма2021_1(Данные, УникальныйИдентификатор)
	ОТД = Новый ОписаниеТипов("Дата");
	ТаблицаОшибок = Новый СписокЗначений;
	УведомлениеОСпецрежимахНалогообложения.ПроверкаАктуальностиФормыПриВыгрузке(
		Данные.Объект.ИмяФормы, ТаблицаОшибок, ПолучитьТаблицуФорм());
	
	Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._Дата) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана дата подписи", "ОсновныеСведения", "ДатаПодписи"));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ИмяФайлТреб) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не заполнено имя файла входящего требования", "ОсновныеСведения", "_ИмяФайлТреб"));
	КонецЕсли;
	УведомлениеОСпецрежимахНалогообложения.ПроверкаКодаНО(Данные.СтруктураРеквизитов._КодНО, ТаблицаОшибок, "ОсновныеСведения", "РегистрацияВИФНС");
	Если ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ОтчетГод)
		И (СтрДлина(Данные.СтруктураРеквизитов._ОтчетГод) <> 4
		Или Не СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Данные.СтруктураРеквизитов._ОтчетГод)) Тогда 
		
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Неправильно указан год", "ОсновныеСведения", "_ОтчетГод"));
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(Данные.Организация) Тогда 
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._НаимОрг) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано наименование организации", "ОсновныеСведения", "_НаимОрг"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ИННЮЛ)
			Или Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(Данные.СтруктураРеквизитов._ИННЮЛ, Истина, "") Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан/неправильно указан ИНН", "ОсновныеСведения", "_ИННЮЛ"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._КПП)
			Или Не РегламентированныеДанныеКлиентСервер.КППСоответствуетТребованиям(Данные.СтруктураРеквизитов._КПП, "") Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан/неправильно указан КПП", "ОсновныеСведения", "_КПП"));
		КонецЕсли;
		
		ФормРеорг = Данные.СтруктураРеквизитов._ФормРеорг;
		Если ФормРеорг = "1" Или ФормРеорг = "2" Или ФормРеорг = "3" Или ФормРеорг = "5" Или ФормРеорг = "6" Тогда 
			Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ИННЮЛРеорг)
				Или Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(Данные.СтруктураРеквизитов._ИННЮЛРеорг, Истина, "") Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан/неправильно указан ИНН реорганизованной организации", "ОсновныеСведения", "_ИННЮЛРеорг"));
			КонецЕсли;
			Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._КППРеорг)
				Или Не РегламентированныеДанныеКлиентСервер.КППСоответствуетТребованиям(Данные.СтруктураРеквизитов._КППРеорг, "") Тогда 
				ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
					"Не указан/неправильно указан КПП реорганизованной организации", "ОсновныеСведения", "_КППРеорг"));
			КонецЕсли;
		КонецЕсли;
	Иначе
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._Фамилия) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указана фамилия", "ОсновныеСведения", "_Фамилия"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._Имя) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано имя", "ОсновныеСведения", "_Имя"));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ИННФЛ)
			Или Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(Данные.СтруктураРеквизитов._ИННФЛ, Ложь, "") Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указан/неправильно указан ИНН", "ОсновныеСведения", "_ИННФЛ"));
		КонецЕсли;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ФамилияПодп) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указана фамилия подписанта", "СведенияОбОтправителе", "_ФамилияПодп"));
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._ИмяПодп) Тогда 
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Не указано имя подписанта", "СведенияОбОтправителе", "_ИмяПодп"));
	КонецЕсли;
	
	Если Данные.СтруктураРеквизитов._ПрПодп = 2 
		Или ЗначениеЗаполнено(Данные.СтруктураРеквизитов._НаимДок) Тогда 
		
		Если Не ЗначениеЗаполнено(Данные.СтруктураРеквизитов._НаимДок) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не указано наименование документа представителя", "СведенияОбОтправителе", "_НаимДок"));
		КонецЕсли;
	КонецЕсли;
	
	Для Каждого Стр Из Данные.СтруктураРеквизитов._РеестрДокПОбНЛ Цикл 
		Если СтрДлина(СокрЛП(Стр.КодОпер)) <> 7 Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не заполнен/неправильно заполнен код операции", "Реестр", "_РеестрДокПОбНЛ", Стр.УИД));
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Стр.ВидОпер) Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не заполнен вид операции", "Реестр", "_РеестрДокПОбНЛ", Стр.УИД));
		КонецЕсли;
		Если Данные.СтруктураРеквизитов._ПредТипДог.Строки.НайтиСтроки(Новый Структура("УИД", Стр.УИД)).Количество() = 0 Тогда 
			ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
				"Не заполнена информация по контрагентам/договорам", "Реестр", "_РеестрДокПОбНЛ", Стр.УИД));
		КонецЕсли;
	КонецЦикла;
	
	Строки = Данные.СтруктураРеквизитов._ПредТипДог.Строки.НайтиСтроки(Новый Структура("ФлагОшибки", Истина), Истина);
	Если Строки.Количество() > 0 Тогда
		Родитель = Строки[0];
		Пока Родитель.Родитель <> Неопределено Цикл 
			Родитель = Родитель.Родитель;
		КонецЦикла;
		ТаблицаОшибок.Добавить(Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьПустуюСтруктуруНавигацииПоОшибкам(
			"Некорректно заполнены реквизиты (выделено красным цветом)", "Реестр", "_РеестрДокПОбНЛ", Родитель.УИД));
	КонецЕсли;
	
	Возврат ТаблицаОшибок;
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления_Форма2020_1(Объект, УникальныйИдентификатор)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	ДанныеОрг = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьСведенияОбОрганизации(Объект);
	
	Если Не ДанныеОрг.ЭтоЮрЛицо Тогда
		ОсновныеСведения.Вставить("ИННФЛ", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("Фамилия", ДанныеОрг.Фамилия);
		ОсновныеСведения.Вставить("Имя", ДанныеОрг.Имя);
		ОсновныеСведения.Вставить("Отчество", ДанныеОрг.Отчество);
	Иначе
		ОсновныеСведения.Вставить("НаимОрг", ДанныеОрг.НаименованиеПолное);
		ОсновныеСведения.Вставить("ИННЮЛ", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("КПП", ДанныеОрг.КПП);
		ОсновныеСведения.Вставить("ИННОрг", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("КППОрг", ДанныеОрг.КПП);
	КонецЕсли;
	
	Документы.УведомлениеОСпецрежимахНалогообложения.ЗаполнитьОбщиеДанные(Объект, ОсновныеСведения);
	Данные = Объект.ДанныеУведомления.Получить();
	СтруктураРеквизитов = Данные.СтруктураРеквизитов;
	Для Каждого КЗ Из СтруктураРеквизитов Цикл 
		Если ТипЗнч(КЗ.Значение) = Тип("Строка")
			Или ТипЗнч(КЗ.Значение) = Тип("Число") Тогда 
			ОсновныеСведения.Вставить(КЗ.Ключ, КЗ.Значение);
		ИначеЕсли ТипЗнч(КЗ.Значение) = Тип("Дата") Тогда
			ОсновныеСведения.Вставить(КЗ.Ключ, Формат(КЗ.Значение, "ДФ=dd.MM.yyyy"));
		КонецЕсли;
	КонецЦикла;
	
	ОсновныеСведения.Вставить("КодНО", ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.РегистрацияВИФНС, "Код"));
	ОсновныеСведения.Вставить("_Дата", Формат(Объект.ДатаПодписи, "ДФ=dd.MM.yyyy"));
	
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления_Форма2020_1(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	
	Возврат ОсновныеСведения;
КонецФункции

Функция ОсновныеСведенияЭлектронногоПредставления_Форма2021_1(Объект, УникальныйИдентификатор)
	ОсновныеСведения = Документы.УведомлениеОСпецрежимахНалогообложения.НачальнаяИнициализацияОбщихРеквизитовВыгрузки(Объект);
	ДанныеОрг = Документы.УведомлениеОСпецрежимахНалогообложения.ПолучитьСведенияОбОрганизации(Объект);
	
	Если Не ДанныеОрг.ЭтоЮрЛицо Тогда
		ОсновныеСведения.Вставить("ИННФЛ", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("Фамилия", ДанныеОрг.Фамилия);
		ОсновныеСведения.Вставить("Имя", ДанныеОрг.Имя);
		ОсновныеСведения.Вставить("Отчество", ДанныеОрг.Отчество);
	Иначе
		ОсновныеСведения.Вставить("НаимОрг", ДанныеОрг.НаименованиеПолное);
		ОсновныеСведения.Вставить("ИННЮЛ", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("КПП", ДанныеОрг.КПП);
		ОсновныеСведения.Вставить("ИННОрг", ДанныеОрг.ИНН);
		ОсновныеСведения.Вставить("КППОрг", ДанныеОрг.КПП);
	КонецЕсли;
	
	Документы.УведомлениеОСпецрежимахНалогообложения.ЗаполнитьОбщиеДанные(Объект, ОсновныеСведения);
	Данные = Объект.ДанныеУведомления.Получить();
	СтруктураРеквизитов = Данные.СтруктураРеквизитов;
	Для Каждого КЗ Из СтруктураРеквизитов Цикл 
		Если ТипЗнч(КЗ.Значение) = Тип("Строка")
			Или ТипЗнч(КЗ.Значение) = Тип("Число") Тогда 
			ОсновныеСведения.Вставить(КЗ.Ключ, КЗ.Значение);
		ИначеЕсли ТипЗнч(КЗ.Значение) = Тип("Дата") Тогда
			ОсновныеСведения.Вставить(КЗ.Ключ, Формат(КЗ.Значение, "ДФ=dd.MM.yyyy"));
		КонецЕсли;
	КонецЦикла;
	
	ОсновныеСведения.Вставить("КодНО", ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.РегистрацияВИФНС, "Код"));
	ОсновныеСведения.Вставить("_Дата", Формат(Объект.ДатаПодписи, "ДФ=dd.MM.yyyy"));
	
	ИдентификаторФайла = ИдентификаторФайлаЭлектронногоПредставления_Форма2021_1(ОсновныеСведения);
	ОсновныеСведения.Вставить("ИдФайл", ИдентификаторФайла);
	
	Возврат ОсновныеСведения;
КонецФункции

Функция ЭлектронноеПредставление_Форма2020_1(Объект, УникальныйИдентификатор)
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2020_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	
	ДатаИмФайла = Формат(ТекущаяДатаСеанса(), "ДФ=_yyyyMMdd_");
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2020_1(Объект, УникальныйИдентификатор);
	Если ОсновныеСведения.ЭтоПБОЮЛ Тогда
		ИННФЛ = ?(ЗначениеЗаполнено(ОсновныеСведения.ИННФЛ), ОсновныеСведения.ИННФЛ, "000000000000");
		ИдентификаторОтправителя = СокрЛП(ИННФЛ);
	Иначе
		ИННЮЛ = ?(ЗначениеЗаполнено(ОсновныеСведения.ИННЮЛ), ОсновныеСведения.ИННЮЛ, "0000000000");
		КПП = ?(ЗначениеЗаполнено(ОсновныеСведения.КПП), ОсновныеСведения.КПП, "000000000");
		ИдентификаторОтправителя = ИННЮЛ + КПП;
	КонецЕсли;
	ОсновныеСведения.Вставить("ДатаИмФайла", ДатаИмФайла);
	ОсновныеСведения.Вставить("ИдентификаторОтправителя", ИдентификаторОтправителя);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2020_1");
	Документы.УведомлениеОСпецрежимахНалогообложения.ОбработатьУсловныеЭлементы(ОсновныеСведения, СтруктураВыгрузки);
	Документы.УведомлениеОСпецрежимахНалогообложения.ЗаполнитьПараметрыСРазделами(ОсновныеСведения, СтруктураВыгрузки);
	ДополнитьПараметры_2020_1(ДанныеУведомления);
	ЗаполнитьДаннымиУзелНов(ДанныеУведомления, СтруктураВыгрузки);
	Документы.УведомлениеОСпецрежимахНалогообложения.ОтсечьНезаполненныеНеобязательныеУзлы(СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Функция ЭлектронноеПредставление_Форма2021_1(Объект, УникальныйИдентификатор)
	ДанныеУведомления = УведомлениеОСпецрежимахНалогообложения.ДанныеУведомленияДляВыгрузки(Объект);
	Ошибки = ПроверитьДокументСВыводомВТаблицу_Форма2021_1(ДанныеУведомления, УникальныйИдентификатор);
	УведомлениеОСпецрежимахНалогообложения.СообщитьОшибкиПриПроверкеВыгрузки(Объект, Ошибки, ДанныеУведомления);
	
	СведенияЭлектронногоПредставления = УведомлениеОСпецрежимахНалогообложения.СведенияЭлектронногоПредставления();
	
	ДатаИмФайла = Формат(ТекущаяДатаСеанса(), "ДФ=_yyyyMMdd_");
	ОсновныеСведения = ОсновныеСведенияЭлектронногоПредставления_Форма2021_1(Объект, УникальныйИдентификатор);
	Если ОсновныеСведения.ЭтоПБОЮЛ Тогда
		ИННФЛ = ?(ЗначениеЗаполнено(ОсновныеСведения.ИННФЛ), ОсновныеСведения.ИННФЛ, "000000000000");
		ИдентификаторОтправителя = СокрЛП(ИННФЛ);
	Иначе
		ИННЮЛ = ?(ЗначениеЗаполнено(ОсновныеСведения.ИННЮЛ), ОсновныеСведения.ИННЮЛ, "0000000000");
		КПП = ?(ЗначениеЗаполнено(ОсновныеСведения.КПП), ОсновныеСведения.КПП, "000000000");
		ИдентификаторОтправителя = ИННЮЛ + КПП;
	КонецЕсли;
	ОсновныеСведения.Вставить("ДатаИмФайла", ДатаИмФайла);
	ОсновныеСведения.Вставить("ИдентификаторОтправителя", ИдентификаторОтправителя);
	СтруктураВыгрузки = Документы.УведомлениеОСпецрежимахНалогообложения.ИзвлечьСтруктуруXMLУведомления(Объект.ИмяОтчета, "СхемаВыгрузкиФорма2021_1");
	Документы.УведомлениеОСпецрежимахНалогообложения.ОбработатьУсловныеЭлементы(ОсновныеСведения, СтруктураВыгрузки);
	Документы.УведомлениеОСпецрежимахНалогообложения.ЗаполнитьПараметрыСРазделами(ОсновныеСведения, СтруктураВыгрузки);
	ДополнитьПараметры_2021_1(ДанныеУведомления);
	ЗаполнитьДаннымиУзелНов(ДанныеУведомления, СтруктураВыгрузки);
	Документы.УведомлениеОСпецрежимахНалогообложения.ОтсечьНезаполненныеНеобязательныеУзлы(СтруктураВыгрузки);
	УведомлениеОСпецрежимахНалогообложения.ВыгрузитьДеревоВТаблицу(СтруктураВыгрузки, ОсновныеСведения, СведенияЭлектронногоПредставления);
	Возврат СведенияЭлектронногоПредставления;
КонецФункции

Процедура ЗаполнитьДаннымиУзелНов(ПараметрыВыгрузки, Узел, ПараметрыТекущейСтраницы = Неопределено, УИДРодителя = Неопределено)
	СтрокиУзла = Новый Массив;
	Для Каждого Стр Из Узел.Строки Цикл
		СтрокиУзла.Добавить(Стр);
	КонецЦикла;
	
	Для Каждого Стр из СтрокиУзла Цикл
		Если Стр.Тип = "А" Или Стр.Тип = "A" Или Стр.Тип = "П" Тогда
			Если ЗначениеЗаполнено(Стр.Ключ) Тогда
				ЗначениеПоказателя = Неопределено;
				Если ПараметрыТекущейСтраницы <> Неопределено И ПараметрыТекущейСтраницы.Свойство(Стр.Ключ, ЗначениеПоказателя) Тогда 
					РегламентированнаяОтчетность.ВывестиПоказательСтатистикиВXML(Стр, ЗначениеПоказателя);
				ИначеЕсли ПараметрыТекущейСтраницы = Неопределено 
					И ЗначениеЗаполнено(Стр.Раздел)
					И ПараметрыВыгрузки.ДанныеУведомления.Свойство(Стр.Раздел, ЗначениеПоказателя) Тогда 
					Если ЗначениеПоказателя.Свойство(Стр.Ключ, ЗначениеПоказателя) Тогда
						РегламентированнаяОтчетность.ВывестиПоказательСтатистикиВXML(Стр, ЗначениеПоказателя);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли Стр.Тип = "С" ИЛИ Стр.Тип = "C" Тогда
			Если Стр.Многостраничность = Истина Тогда
				Многостраничность = Неопределено;
				Если ПараметрыВыгрузки.ДанныеМногостраничныхРазделов.Свойство(Стр.Раздел, Многостраничность)
					И ТипЗнч(Многостраничность) = Тип("СписокЗначений") Тогда
					
					Если ПараметрыВыгрузки.ОптимизированнаяСтруктураВыгрузки.Свойство(Стр.Раздел)
						И ТипЗнч(ПараметрыВыгрузки.ОптимизированнаяСтруктураВыгрузки[Стр.Раздел][УИДРодителя]) = Тип("СписокЗначений") Тогда 
						
						Для Каждого СтрМнгч Из ПараметрыВыгрузки.ОптимизированнаяСтруктураВыгрузки[Стр.Раздел][УИДРодителя] Цикл 
							НовУзел = Документы.УведомлениеОСпецрежимахНалогообложения.НовыйУзелИзПрототипа(Стр);
							ЗаполнитьДаннымиУзелНов(ПараметрыВыгрузки, НовУзел, СтрМнгч.Значение, СтрМнгч.Значение.УИД);
						КонецЦикла;
					Иначе
						Для Каждого СтрМнгч Из Многостраничность Цикл 
							Если УИДРодителя = Неопределено Или СтрМнгч.Значение.УИДРодителя = УИДРодителя Тогда 
								НовУзел = Документы.УведомлениеОСпецрежимахНалогообложения.НовыйУзелИзПрототипа(Стр);
								ЗаполнитьДаннымиУзелНов(ПараметрыВыгрузки, НовУзел, СтрМнгч.Значение, СтрМнгч.Значение.УИД);
							КонецЕсли;
						КонецЦикла;
					КонецЕсли;
				КонецЕсли;
			Иначе
				ЗаполнитьДаннымиУзелНов(ПараметрыВыгрузки, Стр, ПараметрыТекущейСтраницы, УИДРодителя)
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Функция ПустаяСтруктура_РеестрДокПОбНЛ()
	Результат = Новый Структура("УИД, УИДРодителя");
	Результат.Вставить("НомРеестр");
	Результат.Вставить("КодОпер");
	Результат.Вставить("СумНеоблОперВс");
	Результат.Вставить("СумОперВс");
	Возврат Результат;
КонецФункции

Функция ПустаяСтруктура_СведВидОпер()
	Результат = Новый Структура("УИД, УИДРодителя");
	Результат.Вставить("НомСвВидОп");
	Результат.Вставить("ВидОпер");
	Результат.Вставить("СумНеоблОпер");
	Возврат Результат;
КонецФункции

Функция ПустаяСтруктура_ПредТипДог()
	Результат = Новый Структура("УИД, УИДРодителя");
	Результат.Вставить("НомТипДог");
	Результат.Вставить("ИмяФайлДог");
	Возврат Результат;
КонецФункции

Функция ПустаяСтруктура_СведКАгент()
	Результат = Новый Структура("УИД, УИДРодителя");
	Результат.Вставить("СумОпер");
	Результат.Вставить("НаимОрг");
	Результат.Вставить("ИННЮЛ");
	Результат.Вставить("КПП");
	Результат.Вставить("ИННФЛ");
	Результат.Вставить("Фамилия");
	Результат.Вставить("Имя");
	Результат.Вставить("Отчество");
	Возврат Результат;
КонецФункции

Функция ПустаяСтруктура_ДокПОбНЛ()
	Результат = Новый Структура("УИД, УИДРодителя");
	Результат.Вставить("ТипДок");
	Результат.Вставить("НомДок");
	Результат.Вставить("ДатаДок");
	Возврат Результат;
КонецФункции

Процедура ДополнитьДаннымиТиповыхДоговоров(УИДВТаблице, УИДРодителя, СумОперВс, Параметры, НумераторНомТипДог)
	Для Каждого Стр1 Из Параметры.СтруктураРеквизитов._ПредТипДог.Строки.НайтиСтроки(Новый Структура("УИД", УИДВТаблице), Ложь) Цикл 
		НумераторНомТипДог = НумераторНомТипДог + 1;
		УИДПредТипДог = Новый УникальныйИдентификатор;
		ПредТипДог = ПустаяСтруктура_ПредТипДог();
		
		ПредТипДог.УИД = УИДПредТипДог;
		ПредТипДог.УИДРодителя = УИДРодителя;
		ПредТипДог.НомТипДог = НумераторНомТипДог;
		ПредТипДог.ИмяФайлДог = Стр1.ИмяФайлДог;
		
		Для Каждого Стр2 Из Стр1.Строки Цикл
			УИДСведКАгент = Новый УникальныйИдентификатор;
			СведКАгент = ПустаяСтруктура_СведКАгент();
			ЗаполнитьЗначенияСвойств(СведКАгент, Стр2);
			СведКАгент.УИД = УИДСведКАгент;
			СведКАгент.УИДРодителя = УИДПредТипДог;
			СумОперВс = СумОперВс + Стр2.СумОпер;
			
			Для Каждого Стр3 Из Стр2.Строки Цикл
				УИДДокПОбНЛ = Новый УникальныйИдентификатор;
				ДокПОбНЛ = ПустаяСтруктура_ДокПОбНЛ();
				ЗаполнитьЗначенияСвойств(ДокПОбНЛ, Стр3);
				ДокПОбНЛ.УИД = УИДДокПОбНЛ;
				ДокПОбНЛ.УИДРодителя = УИДСведКАгент;
				Параметры.ДанныеМногостраничныхРазделов.ДокПОбНЛ.Добавить(ДокПОбНЛ);
			КонецЦикла;
			
			Параметры.ДанныеМногостраничныхРазделов.СведКАгент.Добавить(СведКАгент);
		КонецЦикла;
		
		Параметры.ДанныеМногостраничныхРазделов.ПредТипДог.Добавить(ПредТипДог);
	КонецЦикла;
КонецПроцедуры

Процедура ДополнитьДаннымиСгруппированныхТиповыхДоговоров(ОтборКодВид, ВсеДоговоры, УИДРодителя, СумОперВс, Параметры)
	НумераторНомТипДог = 0;
	Для Каждого КЗ Из ВсеДоговоры Цикл
		НумераторНомТипДог = НумераторНомТипДог + 1;
		УИДПредТипДог = Новый УникальныйИдентификатор;
		ПредТипДог = ПустаяСтруктура_ПредТипДог();
		
		ПредТипДог.УИД = УИДПредТипДог;
		ПредТипДог.УИДРодителя = УИДРодителя;
		ПредТипДог.НомТипДог = НумераторНомТипДог;
		ПредТипДог.ИмяФайлДог = КЗ.Ключ;
		
		Для Каждого Стр Из ОтборКодВид Цикл 
			Для Каждого Стр1 Из Параметры.СтруктураРеквизитов._ПредТипДог.Строки.НайтиСтроки(Новый Структура("УИД", Стр.УИД), Ложь) Цикл 
				Если Стр1.ИмяФайлДог <> КЗ.Ключ Тогда 
					Продолжить;
				КонецЕсли;
				Для Каждого Стр2 Из Стр1.Строки Цикл
					УИДСведКАгент = Новый УникальныйИдентификатор;
					СведКАгент = ПустаяСтруктура_СведКАгент();
					ЗаполнитьЗначенияСвойств(СведКАгент, Стр2);
					СведКАгент.УИД = УИДСведКАгент;
					СведКАгент.УИДРодителя = УИДПредТипДог;
					СумОперВс = СумОперВс + Стр2.СумОпер;
					
					Для Каждого Стр3 Из Стр2.Строки Цикл
						УИДДокПОбНЛ = Новый УникальныйИдентификатор;
						ДокПОбНЛ = ПустаяСтруктура_ДокПОбНЛ();
						ЗаполнитьЗначенияСвойств(ДокПОбНЛ, Стр3);
						ДокПОбНЛ.УИД = УИДДокПОбНЛ;
						ДокПОбНЛ.УИДРодителя = УИДСведКАгент;
						Параметры.ДанныеМногостраничныхРазделов.ДокПОбНЛ.Добавить(ДокПОбНЛ);
					КонецЦикла;
					
					Параметры.ДанныеМногостраничныхРазделов.СведКАгент.Добавить(СведКАгент);
				КонецЦикла;
			КонецЦикла;
		КонецЦикла;
		Параметры.ДанныеМногостраничныхРазделов.ПредТипДог.Добавить(ПредТипДог);
	КонецЦикла;
КонецПроцедуры

Процедура ДополнитьПараметры_2020_1(Параметры)
	Параметры.Вставить("ДанныеМногостраничныхРазделов", Новый Структура);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("РеестрДокПОбНЛ", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("СведВидОпер", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ПредТипДог", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("СведКАгент", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ДокПОбНЛ", Новый СписокЗначений);
	
	Коды = Новый Соответствие;
	Для Каждого Стр1 Из Параметры.СтруктураРеквизитов._РеестрДокПОбНЛ Цикл 
		Если Не ЗначениеЗаполнено(Стр1.КодОпер) Тогда 
			Продолжить;
		КонецЕсли;
		
		Если Коды[Стр1.КодОпер] = Неопределено Тогда 
			Коды.Вставить(Стр1.КодОпер, Новый Соответствие);
		КонецЕсли;
		Коды[Стр1.КодОпер].Вставить(Стр1.ВидОпер);
	КонецЦикла;
	
	НумераторКод = 0;
	НумераторВид = 0;
	Отбор = Новый Структура("КодОпер, ВидОпер");
	Для Каждого КЗ1 Из Коды Цикл
		УИДКод = Новый УникальныйИдентификатор;
		НумераторКод = НумераторКод + 1;
		РеестрДокПОбНЛ = ПустаяСтруктура_РеестрДокПОбНЛ();
		РеестрДокПОбНЛ.УИД = УИДКод;
		РеестрДокПОбНЛ.НомРеестр = НумераторКод;
		РеестрДокПОбНЛ.КодОпер = КЗ1.Ключ;
		РеестрДокПОбНЛ.СумНеоблОперВс = 0;
		РеестрДокПОбНЛ.СумОперВс = 0;
		
		НумераторВид = 0;
		Для Каждого КЗ2 Из КЗ1.Значение Цикл
			УИДВид = Новый УникальныйИдентификатор;
			НумераторВид = НумераторВид + 1;
			
			СведВидОпер = ПустаяСтруктура_СведВидОпер();
			СведВидОпер.УИД = УИДВид;
			СведВидОпер.УИДРодителя = УИДКод;
			СведВидОпер.НомСвВидОп = НумераторВид;
			СведВидОпер.ВидОпер = КЗ2.Ключ;
			СведВидОпер.СумНеоблОпер = 0;
			
			Отбор["КодОпер"] = КЗ1.Ключ;
			Отбор["ВидОпер"] = КЗ2.Ключ;
			
			НумераторНомТипДог = 0;
			Для Каждого Стр Из Параметры.СтруктураРеквизитов._РеестрДокПОбНЛ.НайтиСтроки(Отбор) Цикл 
				СведВидОпер.СумНеоблОпер = СведВидОпер.СумНеоблОпер + Стр.СумНеоблОпер;
				РеестрДокПОбНЛ.СумНеоблОперВс = РеестрДокПОбНЛ.СумНеоблОперВс + Стр.СумНеоблОпер;
				
				ДополнитьДаннымиТиповыхДоговоров(Стр.УИД, УИДВид, РеестрДокПОбНЛ.СумОперВс, Параметры, НумераторНомТипДог);
			КонецЦикла;
			
			Параметры.ДанныеМногостраничныхРазделов.СведВидОпер.Добавить(СведВидОпер);
		КонецЦикла;
		
		Параметры.ДанныеМногостраничныхРазделов.РеестрДокПОбНЛ.Добавить(РеестрДокПОбНЛ);
	КонецЦикла;
	
	ОптимизированнаяСтруктураВыгрузки = Новый Структура;
	Параметры.Вставить("ОптимизированнаяСтруктураВыгрузки", ОптимизированнаяСтруктураВыгрузки);
КонецПроцедуры

Процедура ДополнитьПараметры_2021_1(Параметры)
	Параметры.Вставить("ДанныеМногостраничныхРазделов", Новый Структура);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("РеестрДокПОбНЛ", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("СведВидОпер", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ПредТипДог", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("СведКАгент", Новый СписокЗначений);
	Параметры.ДанныеМногостраничныхРазделов.Вставить("ДокПОбНЛ", Новый СписокЗначений);
	
	Коды = Новый Соответствие;
	Для Каждого Стр1 Из Параметры.СтруктураРеквизитов._РеестрДокПОбНЛ Цикл 
		Если Не ЗначениеЗаполнено(Стр1.КодОпер) Тогда 
			Продолжить;
		КонецЕсли;
		
		Если Коды[Стр1.КодОпер] = Неопределено Тогда 
			Коды.Вставить(Стр1.КодОпер, Новый Соответствие);
		КонецЕсли;
		Коды[Стр1.КодОпер].Вставить(Стр1.ВидОпер);
	КонецЦикла;
	
	НумераторКод = 0;
	НумераторВид = 0;
	Отбор = Новый Структура("КодОпер, ВидОпер");
	Для Каждого КЗ1 Из Коды Цикл
		УИДКод = Новый УникальныйИдентификатор;
		НумераторКод = НумераторКод + 1;
		РеестрДокПОбНЛ = ПустаяСтруктура_РеестрДокПОбНЛ();
		РеестрДокПОбНЛ.УИД = УИДКод;
		РеестрДокПОбНЛ.НомРеестр = НумераторКод;
		РеестрДокПОбНЛ.КодОпер = КЗ1.Ключ;
		РеестрДокПОбНЛ.СумНеоблОперВс = 0;
		РеестрДокПОбНЛ.СумОперВс = 0;
		
		НумераторВид = 0;
		Для Каждого КЗ2 Из КЗ1.Значение Цикл
			УИДВид = Новый УникальныйИдентификатор;
			НумераторВид = НумераторВид + 1;
			
			СведВидОпер = ПустаяСтруктура_СведВидОпер();
			СведВидОпер.УИД = УИДВид;
			СведВидОпер.УИДРодителя = УИДКод;
			СведВидОпер.НомСвВидОп = НумераторВид;
			СведВидОпер.ВидОпер = КЗ2.Ключ;
			СведВидОпер.СумНеоблОпер = 0;
			
			Отбор["КодОпер"] = КЗ1.Ключ;
			Отбор["ВидОпер"] = КЗ2.Ключ;
			
			ОтборКодВид = Параметры.СтруктураРеквизитов._РеестрДокПОбНЛ.НайтиСтроки(Отбор);
			ВсеДоговоры = Новый Соответствие;
			Для Каждого Стр Из ОтборКодВид Цикл 
				Для Каждого Стр1 Из Параметры.СтруктураРеквизитов._ПредТипДог.Строки.НайтиСтроки(Новый Структура("УИД", Стр.УИД), Ложь) Цикл 
					Если ВсеДоговоры[Стр1.ИмяФайлДог] = Неопределено Тогда 
						ВсеДоговоры.Вставить(Стр1.ИмяФайлДог, Стр.УИД);
					КонецЕсли;
				КонецЦикла;
			КонецЦикла;
			
			Для Каждого Стр Из ОтборКодВид Цикл 
				СведВидОпер.СумНеоблОпер = СведВидОпер.СумНеоблОпер + Стр.СумНеоблОпер;
				РеестрДокПОбНЛ.СумНеоблОперВс = РеестрДокПОбНЛ.СумНеоблОперВс + Стр.СумНеоблОпер;
			КонецЦикла;
			
			Если ОтборКодВид.Количество() = 1 Тогда 
				НумераторНомТипДог = 0;
				Стр = ОтборКодВид[0];
				ДополнитьДаннымиТиповыхДоговоров(Стр.УИД, УИДВид, РеестрДокПОбНЛ.СумОперВс, Параметры, НумераторНомТипДог);
			Иначе
				ДополнитьДаннымиСгруппированныхТиповыхДоговоров(ОтборКодВид, ВсеДоговоры, УИДВид, РеестрДокПОбНЛ.СумОперВс, Параметры);
			КонецЕсли;
			
			Параметры.ДанныеМногостраничныхРазделов.СведВидОпер.Добавить(СведВидОпер);
		КонецЦикла;
		
		Параметры.ДанныеМногостраничныхРазделов.РеестрДокПОбНЛ.Добавить(РеестрДокПОбНЛ);
	КонецЦикла;
	
	ОптимизированнаяСтруктураВыгрузки = Новый Структура;
	Для Каждого Элт Из Параметры.ДанныеМногостраничныхРазделов Цикл 
		ОптимизированнаяСтруктураВыгрузки.Вставить(Элт.Ключ, Новый Соответствие);
		Для Каждого Стр Из Параметры.ДанныеМногостраничныхРазделов[Элт.Ключ] Цикл 
			УИДРодителя = Стр.Значение.УИДРодителя;
			Если УИДРодителя = Неопределено Тогда 
				Продолжить;
			КонецЕсли;
			Если ОптимизированнаяСтруктураВыгрузки[Элт.Ключ][УИДРодителя] = Неопределено Тогда 
				ОптимизированнаяСтруктураВыгрузки[Элт.Ключ].Вставить(УИДРодителя, Новый СписокЗначений);
			КонецЕсли;
			ОптимизированнаяСтруктураВыгрузки[Элт.Ключ][УИДРодителя].Добавить(Стр.Значение);
		КонецЦикла;
		Если ОптимизированнаяСтруктураВыгрузки[Элт.Ключ].Количество() = 0 Тогда 
			ОптимизированнаяСтруктураВыгрузки.Удалить(Элт.Ключ);
		КонецЕсли;
	КонецЦикла;
	Параметры.Вставить("ОптимизированнаяСтруктураВыгрузки", ОптимизированнаяСтруктураВыгрузки);
КонецПроцедуры

Процедура ПроверкаОтчета(ПараметрыОтчета, АдресХранилища) Экспорт
	Если ПараметрыОтчета.ИДРедакцииОтчета = "Форма2021_1" Тогда 
		ПроверкаОтчета_Форма2021_1(ПараметрыОтчета._ПредТипДог);
		ПоместитьВоВременноеХранилище(Новый Структура("_ПредТипДог", ПараметрыОтчета._ПредТипДог), АдресХранилища);
	КонецЕсли;
КонецПроцедуры

Процедура ПроверкаОтчета_Форма2021_1(_ПредТипДог)
	Для Каждого Стр0 Из _ПредТипДог.Строки Цикл 
		Если Стр0.Строки.Количество() = 0 Тогда 
			Стр0.ОписаниеКА = "<Необходимо добавить контрагентов>";
			Стр0.ФлагОшибки = Истина;
		КонецЕсли;
		
		Для Каждого Стр1 Из Стр0.Строки Цикл 
			Если Стр1.Строки.Количество() = 0 Тогда 
				Стр1.ОписаниеКА = "<Необходимо добавить документы>";
				Стр1.ФлагОшибки = Истина;
			Иначе
				Если ЗначениеЗаполнено(Стр1.ИННЮЛ) 
					Или ЗначениеЗаполнено(Стр1.КПП)
					Или ЗначениеЗаполнено(Стр1.НаимОрг) Тогда 
					
					Если ЗначениеЗаполнено(Стр1.ИННЮЛ) 
						И Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(Стр1.ИННЮЛ, Истина, "") Тогда 
						Стр1.ОписаниеКА = "<Некорректно заполнен ИНН>";
						Стр1.ФлагОшибки = Истина;
					ИначеЕсли ЗначениеЗаполнено(Стр1.КПП) 
						И Не РегламентированныеДанныеКлиентСервер.КППСоответствуетТребованиям(Стр1.КПП, "") Тогда 
						
						Стр1.ОписаниеКА = "<Некорректно заполнен КПП>";
						Стр1.ФлагОшибки = Истина;
					ИначеЕсли Не ЗначениеЗаполнено(Стр1.НаимОрг) Тогда  
						Стр1.ОписаниеКА = "<Не указано наименование организации>";
						Стр1.ФлагОшибки = Истина;
					КонецЕсли;
				Иначе
					Если Не ЗначениеЗаполнено(Стр1.Фамилия) Или Не ЗначениеЗаполнено(Стр1.Имя) Тогда
						Стр1.ОписаниеКА = "<Не указаны имя и фамилия>";
						Стр1.ФлагОшибки = Истина;
					ИначеЕсли ЗначениеЗаполнено(Стр1.ИННФЛ)
						И Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(Стр1.ИННФЛ, Ложь, "") Тогда 
						
						Стр1.ОписаниеКА = "<Некорректно заполнен ИНН>";
						Стр1.ФлагОшибки = Истина;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
			
			Для Каждого Стр2 Из Стр1.Строки Цикл 
				Если Не ЗначениеЗаполнено(Стр2.ТипДок)
					Или Не ЗначениеЗаполнено(Стр2.НомДок)
					Или Не ЗначениеЗаполнено(Стр2.ДатаДок) Тогда 
					
					Стр2.ОписаниеКА = "<Необходимо заполнить реквизиты документа>";
					Стр2.ФлагОшибки = Истина;
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры

#КонецОбласти

#Область Загрузка
Процедура ПостОбработка(КонтекстЗаполнения)
	Для Каждого Стр1 Из КонтекстЗаполнения._ПредТипДог.Строки Цикл 
		Если Стр1.Строки.Количество() = 0 Тогда 
			НовСтр = Стр1.Строки.Добавить();
			НовСтр.УИД = Стр1.УИД;
			НовСтр.УИДЛок = Новый УникальныйИдентификатор;
			НовСтр.ФлагОшибки = Истина;
			НовСтр.ОписаниеКА = "<Необходимо заполнить реквизиты контрагента>";
			
			НовСтр = НовСтр.Строки.Добавить();
			НовСтр.УИД = Стр1.УИД;
			НовСтр.УИДЛок = Новый УникальныйИдентификатор;
			НовСтр.ФлагОшибки = Истина;
			НовСтр.ОписаниеКА = "<Необходимо заполнить реквизиты документа>";
			Продолжить;
		КонецЕсли;
		
		Для Каждого Стр2 Из Стр1.Строки Цикл 
			Если Стр2.Строки.Количество() = 0 Тогда 
				НовСтр = Стр2.Строки.Добавить();
				НовСтр.УИД = Стр1.УИД;
				НовСтр.УИДЛок = Новый УникальныйИдентификатор;
				НовСтр.ФлагОшибки = Истина;
				НовСтр.ОписаниеКА = "<Необходимо заполнить реквизиты документа>";
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры

Процедура СформироватьСоответствиеКодВид(КопированныйКонтекст)
	Результат = Новый Соответствие;
	Для Каждого Стр Из КопированныйКонтекст._РеестрДокПОбНЛ Цикл 
		Результат.Вставить(ХешКодВид(Стр.КодОпер, Стр.ВидОпер), Стр);
	КонецЦикла;
	КопированныйКонтекст.Вставить("СтрокиКодВид", Результат);
КонецПроцедуры

Процедура КонсолидироватьВФоне(КонтекстЗаполнения, АдресХранилища) Экспорт
	КопированныйКонтекст = ОбщегоНазначенияКлиентСервер.СкопироватьСтруктуру(КонтекстЗаполнения);
	СформироватьСоответствиеКодВид(КопированныйКонтекст);
	
	Для Каждого СтрНаФормеЗагрузки Из КопированныйКонтекст.ФайлыВыгрузки Цикл
		ДеревоЗагрузки = СтрНаФормеЗагрузки.Дерево;
		
		КопированныйКонтекст.ОтборВДереве.ИмяЭлемента = "РеестрДокПОбНЛ";
		СтрокаПоКоду = УведомлениеОСпецрежимахНалогообложения.НайтиУзелВДеревеПоПути(ДеревоЗагрузки, "Файл/Документ/РеестрДокПОбНЛ", "ИмяЭлемента");
		Если СтрокаПоКоду = Неопределено Тогда 
			Продолжить;
		КонецЕсли;
		СтрокиПоКоду = СтрокаПоКоду.Родитель.Строки.НайтиСтроки(КопированныйКонтекст.ОтборВДереве, Ложь);
		Для Каждого Стр Из СтрокиПоКоду Цикл
			КопированныйКонтекст.ОтборВДереве.ИмяЭлемента = "СведВидОпер";
			Для Каждого СтрПоВиду Из Стр.Строки.НайтиСтроки(КопированныйКонтекст.ОтборВДереве, Ложь) Цикл 
				СтрКодОпер = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(Стр.Строки, "КодОпер", "ИмяЭлемента");
				СтрВидОпер = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрПоВиду.Строки, "ВидОпер", "ИмяЭлемента");
				
				Если СтрКодОпер = Неопределено Или СтрВидОпер = Неопределено Тогда 
					Продолжить;
				КонецЕсли;
				
				СтрокаВДеревеКодВид = СтрокаВДеревеПоКодВид(КопированныйКонтекст, СтрКодОпер.ЗначениеЭлемента, СтрВидОпер.ЗначениеЭлемента);
				СумНеоблОпер = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрПоВиду.Строки, "ОбщСтНеоблОп", "ИмяЭлемента");
				СтрокаВДеревеКодВид.СумНеоблОпер = СтрокаВДеревеКодВид.СумНеоблОпер + КопированныйКонтекст.ОТЧ.ПривестиЗначение(СумНеоблОпер.ЗначениеЭлемента);
				ДополнитьРасшифровкуПоКодВид(КопированныйКонтекст, СтрокаВДеревеКодВид.УИД, СтрПоВиду);
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
	
	ПостОбработка(КопированныйКонтекст);
	ПоместитьВоВременноеХранилище(КопированныйКонтекст, АдресХранилища);
КонецПроцедуры

Процедура ДополнитьРасшифровкуПоКодВид(КонтекстЗаполнения, ИдентификаторСтрокиКодВид, СтрПоВиду)
	КонтекстЗаполнения.ОтборВДереве.ИмяЭлемента = "ПредТипДог";
	Для Каждого СтрДоговор Из СтрПоВиду.Строки.НайтиСтроки(КонтекстЗаполнения.ОтборВДереве) Цикл 
		ИмяФайлДог = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрДоговор.Строки, "ИмяФайлДог", "ИмяЭлемента");
		Если ИмяФайлДог = Неопределено Тогда
			ИмяФайлДог = "";
		Иначе
			ИмяФайлДог = ИмяФайлДог.ЗначениеЭлемента;
		КонецЕсли;
		
		СтрокиПоДоговору = КонтекстЗаполнения._ПредТипДог.Строки.НайтиСтроки(Новый Структура("УИД, ИмяФайлДог", ИдентификаторСтрокиКодВид, ИмяФайлДог), Ложь);
		Если СтрокиПоДоговору.Количество() = 0 Тогда
			СтрокаПоДоговору = КонтекстЗаполнения._ПредТипДог.Строки.Добавить();
			СтрокаПоДоговору.УИД = ИдентификаторСтрокиКодВид;
			СтрокаПоДоговору.УИДЛок = Новый УникальныйИдентификатор;
			СтрокаПоДоговору.ИмяФайлДог = ИмяФайлДог;
			СтрокаПоДоговору.ОписаниеКА = ?(ЗначениеЗаполнено(ИмяФайлДог), ИмяФайлДог, "<типовой договор не представлен>");
		Иначе
			СтрокаПоДоговору = СтрокиПоДоговору[0];
		КонецЕсли;
		
		ДополнитьРасшифровкуПоТиповомуДоговору(КонтекстЗаполнения, ИдентификаторСтрокиКодВид, СтрокаПоДоговору, СтрДоговор);
	КонецЦикла;
КонецПроцедуры

Процедура ДополнитьРасшифровкуПоТиповомуДоговору(КонтекстЗаполнения, ИдентификаторСтрокиКодВид, СтрокаПоДоговору, СтрДоговор)
	КонтекстЗаполнения.ОтборВДереве.ИмяЭлемента = "СведКАгент";
	Для Каждого СтрКА Из СтрДоговор.Строки.НайтиСтроки(КонтекстЗаполнения.ОтборВДереве) Цикл
		СтруктураОтбораКА = Новый Структура("УИД", ИдентификаторСтрокиКодВид);
		
		СведОрг = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрКА.Строки, "СведОрг", "ИмяЭлемента");
		Если СведОрг = Неопределено Тогда 
			СведОрг = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрКА.Строки, "СведФЛ", "ИмяЭлемента");
			Если СведОрг = Неопределено Тогда 
				Продолжить;
			КонецЕсли;
			
			Фамилия = "";
			Имя = "";
			Отчество = "";
			ИННФЛ = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СведОрг.Строки, "ИННФЛ", "ИмяЭлемента");
			ИННФЛ = ?(ИННФЛ = Неопределено, "", ИННФЛ.ЗначениеЭлемента);
			
			ФИО = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СведОрг.Строки, "ФИО", "ИмяЭлемента");
			Если ТипЗнч(ФИО) = Тип("СтрокаДереваЗначений") Тогда 
				Фамилия = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(ФИО.Строки, "Фамилия", "ИмяЭлемента");
				Имя = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(ФИО.Строки, "Имя", "ИмяЭлемента");
				Отчество = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(ФИО.Строки, "Отчество", "ИмяЭлемента");
				Фамилия = ?(Фамилия = Неопределено, "", Фамилия.ЗначениеЭлемента);
				Имя = ?(Имя = Неопределено, "", Имя.ЗначениеЭлемента);
				Отчество = ?(Отчество = Неопределено, "", Отчество.ЗначениеЭлемента);
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ИННФЛ) Тогда 
				СтруктураОтбораКА.Вставить("ИННФЛ", ИННФЛ);
			ИначеЕсли ТипЗнч(ФИО) = Тип("СтрокаДереваЗначений") Тогда 
				СтруктураОтбораКА.Вставить("Фамилия", Фамилия);
				СтруктураОтбораКА.Вставить("Имя", Имя);
				СтруктураОтбораКА.Вставить("Отчество", Отчество);
			Иначе
				СтруктураОтбораКА.УИД = Новый УникальныйИдентификатор;
			КонецЕсли;
			
			ОтбСтроки = СтрокаПоДоговору.Строки.НайтиСтроки(СтруктураОтбораКА, Ложь);
			Если ОтбСтроки.Количество() = 0 Тогда 
				СтрокаПоКонтрагенту = СтрокаПоДоговору.Строки.Добавить();
				СтрокаПоКонтрагенту.УИД = ИдентификаторСтрокиКодВид;
				СтрокаПоКонтрагенту.УИДЛок = Новый УникальныйИдентификатор;
				СтрокаПоКонтрагенту.ИННФЛ = ИННФЛ;
				СтрокаПоКонтрагенту.Фамилия = Фамилия;
				СтрокаПоКонтрагенту.Имя = Имя;
				СтрокаПоКонтрагенту.Отчество = Отчество;
				СформироватьОписаниеКонтрагента(СтрокаПоКонтрагенту, Ложь);
			Иначе
				СтрокаПоКонтрагенту = ОтбСтроки[0];
			КонецЕсли;
		Иначе
			ИННЮЛ = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СведОрг.Строки, "ИННЮЛ", "ИмяЭлемента");
			КПП = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СведОрг.Строки, "КПП", "ИмяЭлемента");
			НаимОрг = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СведОрг.Строки, "НаимОрг", "ИмяЭлемента");
			
			ИННЮЛ = ?(ИННЮЛ = Неопределено, "", ИННЮЛ.ЗначениеЭлемента);
			КПП = ?(КПП = Неопределено, "", КПП.ЗначениеЭлемента);
			НаимОрг = ?(НаимОрг = Неопределено, "", НаимОрг.ЗначениеЭлемента);
			Если ЗначениеЗаполнено(ИННЮЛ) И ЗначениеЗаполнено(КПП) Тогда 
				СтруктураОтбораКА.Вставить("ИННЮЛ", ИННЮЛ);
				СтруктураОтбораКА.Вставить("КПП", КПП);
			Иначе
				СтруктураОтбораКА.Вставить("НаимОрг", НаимОрг);
			КонецЕсли;
			
			ОтбСтроки = СтрокаПоДоговору.Строки.НайтиСтроки(СтруктураОтбораКА, Ложь);
			Если ОтбСтроки.Количество() = 0 Тогда 
				СтрокаПоКонтрагенту = СтрокаПоДоговору.Строки.Добавить();
				СтрокаПоКонтрагенту.УИД = ИдентификаторСтрокиКодВид;
				СтрокаПоКонтрагенту.УИДЛок = Новый УникальныйИдентификатор;
				СтрокаПоКонтрагенту.ИННЮЛ = ИННЮЛ;
				СтрокаПоКонтрагенту.КПП = КПП;
				СтрокаПоКонтрагенту.НаимОрг = НаимОрг;
				СформироватьОписаниеКонтрагента(СтрокаПоКонтрагенту, Истина);
			Иначе
				СтрокаПоКонтрагенту = ОтбСтроки[0];
			КонецЕсли;
		КонецЕсли;
			
		ОбщСтТов = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрКА.Строки, "ОбщСтТов", "ИмяЭлемента");
		СтрокаПоКонтрагенту.СумОпер = СтрокаПоКонтрагенту.СумОпер 
			+ КонтекстЗаполнения.ОТЧ.ПривестиЗначение(?(ОбщСтТов = Неопределено, "", ОбщСтТов.ЗначениеЭлемента));
			
		ДополнитьРасшифровкуПоДокументу(КонтекстЗаполнения, ИдентификаторСтрокиКодВид, СтрокаПоКонтрагенту, СтрКА);
	КонецЦикла;
КонецПроцедуры

Процедура ДополнитьРасшифровкуПоДокументу(КонтекстЗаполнения, ИдентификаторСтрокиКодВид, СтрокаПоКонтрагенту, СтрКА)
	КонтекстЗаполнения.ОтборВДереве.ИмяЭлемента = "ДокПОбНЛ";
	Для Каждого СтрДоговор Из СтрКА.Строки.НайтиСтроки(КонтекстЗаполнения.ОтборВДереве, Ложь) Цикл 
		СтруктураОтбораКА = Новый Структура("УИД", ИдентификаторСтрокиКодВид);
		
		ТипДок = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрДоговор.Строки, "ВидДок", "ИмяЭлемента");
		НомДок = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрДоговор.Строки, "НомДок", "ИмяЭлемента");
		ДатаДок = УведомлениеОСпецрежимахНалогообложения.ПолучитьПодчиненныйЭлемент(СтрДоговор.Строки, "ДатаДок", "ИмяЭлемента");
		ТипДок = ?(ТипДок = Неопределено, "", ТипДок.ЗначениеЭлемента);
		НомДок = ?(НомДок = Неопределено, "", НомДок.ЗначениеЭлемента);
		ДатаДок = ?(ДатаДок = Неопределено, "", ДатаДок.ЗначениеЭлемента);
		ДатаДок = РегламентированнаяОтчетностьЗагрузка.ДатаИзСтрокиЛюбогоФормата(ДатаДок);
		
		СтруктураОтбораКА.Вставить("ТипДок", ТипДок);
		СтруктураОтбораКА.Вставить("НомДок", НомДок);
		СтруктураОтбораКА.Вставить("ДатаДок", ДатаДок);
		
		ОтбСтроки = СтрокаПоКонтрагенту.Строки.НайтиСтроки(СтруктураОтбораКА, Ложь);
		Если ОтбСтроки.Количество() = 0 Тогда
			СтрокаПоДокументу = СтрокаПоКонтрагенту.Строки.Добавить();
			СтрокаПоДокументу.УИД = ИдентификаторСтрокиКодВид;
			СтрокаПоДокументу.УИДЛок = Новый УникальныйИдентификатор;
			СтрокаПоДокументу.ТипДок = ТипДок;
			СтрокаПоДокументу.НомДок = НомДок;
			СтрокаПоДокументу.ДатаДок = ДатаДок;
			СформироватьОписаниеДокумента(СтрокаПоДокументу);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура СформироватьОписаниеДокумента(СтрокаПоДокументу)
	Если ЗначениеЗаполнено(СтрокаПоДокументу.ДатаДок) И Год (СтрокаПоДокументу.ДатаДок) < 1900 Тогда
		СтрокаПоДокументу.ОписаниеКА = "<Некорректно указана дата документа>";
		СтрокаПоДокументу.ФлагОшибки = Истина;
	ИначеЕсли ЗначениеЗаполнено(СтрокаПоДокументу.ТипДок)
			И ЗначениеЗаполнено(СтрокаПоДокументу.НомДок) 
			И ЗначениеЗаполнено(СтрокаПоДокументу.ДатаДок) Тогда 
		СтрокаПоДокументу.ОписаниеКА = СтрокаПоДокументу.ТипДок + " №" 
			+ ?(ЗначениеЗаполнено(СтрокаПоДокументу.НомДок), СтрокаПоДокументу.НомДок, "б/н") 
			+ " от " + Формат(СтрокаПоДокументу.ДатаДок, "ДЛФ=DD; ДП=-");
	Иначе
		СтрокаПоДокументу.ОписаниеКА = "<Необходимо заполнить реквизиты документа>";
		СтрокаПоДокументу.ФлагОшибки = Истина;
	КонецЕсли;
КонецПроцедуры

Процедура СформироватьОписаниеКонтрагента(СтрокаПоКонтрагенту, ЭтоЮЛ)
	Если ЭтоЮЛ Тогда 
		Если ЗначениеЗаполнено(СтрокаПоКонтрагенту.ИННЮЛ) 
			И Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(СтрокаПоКонтрагенту.ИННЮЛ, Истина, "") Тогда 
			
			СтрокаПоКонтрагенту.ОписаниеКА = "<Некорректно заполнен ИНН>";
			СтрокаПоКонтрагенту.ФлагОшибки = Истина;
		ИначеЕсли ЗначениеЗаполнено(СтрокаПоКонтрагенту.КПП) 
			И Не РегламентированныеДанныеКлиентСервер.КППСоответствуетТребованиям(СтрокаПоКонтрагенту.КПП, "") Тогда 
			
			СтрокаПоКонтрагенту.ОписаниеКА = "<Некорректно заполнен КПП>";
			СтрокаПоКонтрагенту.ФлагОшибки = Истина;
		ИначеЕсли ЗначениеЗаполнено(СтрокаПоКонтрагенту.НаимОрг) Тогда 
			СтрокаПоКонтрагенту.ОписаниеКА = СтрокаПоКонтрагенту.НаимОрг 
				+ ", ИНН/КПП: " + СтрокаПоКонтрагенту.ИННЮЛ + "/" + СтрокаПоКонтрагенту.КПП;
		Иначе
			СтрокаПоКонтрагенту.ОписаниеКА = "<Необходимо заполнить реквизиты контрагента>";
			СтрокаПоКонтрагенту.ФлагОшибки = Истина;
		КонецЕсли;
	Иначе
		Если ЗначениеЗаполнено(СтрокаПоКонтрагенту.ИННФЛ) 
				И Не РегламентированныеДанныеКлиентСервер.ИННСоответствуетТребованиям(СтрокаПоКонтрагенту.ИННФЛ, Ложь, "") Тогда 
			СтрокаПоКонтрагенту.ОписаниеКА = "<Некорректно заполнен ИНН>";
			СтрокаПоКонтрагенту.ФлагОшибки = Истина;
		ИначеЕсли ЗначениеЗаполнено(СтрокаПоКонтрагенту.Фамилия) И ЗначениеЗаполнено(СтрокаПоКонтрагенту.Имя) Тогда 
			СтрокаПоКонтрагенту.ОписаниеКА = СтрокаПоКонтрагенту.Фамилия + " " + СтрокаПоКонтрагенту.Имя + " " + СтрокаПоКонтрагенту.Отчество 
				+ ?(ЗначениеЗаполнено(СтрокаПоКонтрагенту.ИННФЛ), ", ИНН: " + СтрокаПоКонтрагенту.ИННФЛ, "");
		Иначе
			СтрокаПоКонтрагенту.ОписаниеКА = "<Необходимо заполнить фамилию и имя>";
			СтрокаПоКонтрагенту.ФлагОшибки = Истина;
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Функция СтрокаВДеревеПоКодВид(КонтекстЗаполнения, КодОпер, ВидОпер)
	Хеш = ХешКодВид(КодОпер, ВидОпер);
	Если КонтекстЗаполнения.СтрокиКодВид[Хеш] = Неопределено Тогда 
		НовСтр = КонтекстЗаполнения._РеестрДокПОбНЛ.Добавить();
		НовСтр.УИД = Новый УникальныйИдентификатор;
		НовСтр.КодОпер = КодОпер;
		НовСтр.ВидОпер = ВидОпер;
		ОтбОписание = КонтекстЗаполнения.СпискиВыбораКодов2022.НайтиСтроки(Новый Структура("Код", КодОпер));
		Если ОтбОписание.Количество() > 0 Тогда 
			НовСтр.ОписаниеОперации = ОтбОписание[0].Название;
		КонецЕсли;
		КонтекстЗаполнения.СтрокиКодВид[Хеш] = НовСтр;
	КонецЕсли;
	Возврат КонтекстЗаполнения.СтрокиКодВид[Хеш];
КонецФункции

Функция ХешКодВид(Код, Вид)
	Возврат СтрЗаменить(СтрЗаменить(НРег(Код + Вид), " ", ""), ",", "");
КонецФункции

#КонецОбласти
#КонецЕсли