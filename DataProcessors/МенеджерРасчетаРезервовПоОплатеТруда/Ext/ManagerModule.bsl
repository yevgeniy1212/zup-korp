#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область СлужебныйПрограммныйИнтерфейс

#Область КонструкторыКоллекций

// Конструктор таблицы показателей, используемой в коллекциях Зарплата.Начисления, Зарплата.Удержания.
//
Функция ТаблицаПоказателей() Экспорт
		
	Возврат Обработки.МенеджерРасчетаЗарплаты.ТаблицаПоказателей();
	
КонецФункции

// Конструктор строки таблицы начислений коллекции типа Зарплата.Начисления.
//
// Параметры
//	ТаблицаНачислений - тип ТаблицаЗначений, таблица, в которой создается строка.
//		Должна быть создана конструктором ТаблицаНачислений().
//
Функция СтрокаТаблицыНачислений(ТаблицаНачислений) Экспорт
	
	Возврат Обработки.МенеджерРасчетаЗарплаты.СтрокаТаблицыНачислений(ТаблицаНачислений);
	
КонецФункции

// Конструктор таблицы начислений, используется для заполнения параметров других методов.
//
Функция ТаблицаНачислений() Экспорт
	
	Возврат РезервыПоОплатеТруда.ТаблицаНачисления();
	
КонецФункции

// Создает пустую таблицу взносы.
Функция ТаблицаВзносы() Экспорт
	
	Возврат РезервыПоОплатеТруда.ТаблицаВзносы();
	
КонецФункции

// Конструктор таблицы расчета резервов, используется для заполнения параметров других методов.
//
Функция ТаблицаРасчетРезервов() Экспорт
	
	Возврат РезервыПоОплатеТруда.ТаблицаРасчетРезервов();
	
КонецФункции

// Конструктор таблицы резервов по сотрудникам, используется для заполнения параметров других методов.
//
Функция ТаблицаОценочныеОбязательстваПоСотрудникам() Экспорт
	
	Возврат РезервыПоОплатеТруда.ТаблицаОценочныеОбязательстваПоСотрудникам();
	
КонецФункции

// Конструктор сводной таблицы резервов, используется для заполнения параметров других методов.
//
Функция ТаблицаОценочныеОбязательства() Экспорт
	
	Возврат РезервыПоОплатеТруда.ТаблицаОценочныеОбязательства();
	
КонецФункции

#КонецОбласти

#КонецОбласти

#КонецЕсли