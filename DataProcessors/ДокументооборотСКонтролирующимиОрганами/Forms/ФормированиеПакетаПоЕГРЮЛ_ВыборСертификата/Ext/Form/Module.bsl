
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.ДекорацияОписание.Заголовок = 
		СтроковыеФункции.ФорматированнаяСтрока(
			НСтр("ru = '<span style=""font:ШрифтЖирныйБРО"">Пакет успешно сформирован.</span>
			|
			|Выберите сертификат учредителя от лица которого производится отправка в ИФНС.'"));
	
	ВсеСертификаты = Параметры.ВыбранныеСертификаты;
	Для Счетчик = 0 По ВсеСертификаты.Количество() - 1 Цикл
		СписокСертификатов.Добавить(Счетчик, ВсеСертификаты[Счетчик]);
	КонецЦикла;
	
	Если СписокСертификатов.Количество() > 0 Тогда
		Элементы.СписокСертификатов.ТекущаяСтрока = СписокСертификатов[0].ПолучитьИдентификатор();
		ТекущийЭлемент = Элементы.СписокСертификатов;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписокСертификатов

&НаКлиенте
Процедура СписокСертификатовВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ТекущаяСтрока = Элементы.СписокСертификатов.ТекущиеДанные;
	СформироватьРезультат(ТекущаяСтрока.Значение);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	ТекущаяСтрока = Элементы.СписокСертификатов.ТекущиеДанные;
	Если ТекущаяСтрока <> Неопределено Тогда
		СформироватьРезультат(ТекущаяСтрока.Значение);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура СформироватьРезультат(ТекущееЗначение)
	
	Закрыть(ТекущееЗначение);
	
КонецПроцедуры

#КонецОбласти

