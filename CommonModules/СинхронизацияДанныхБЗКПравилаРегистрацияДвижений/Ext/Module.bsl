#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// См. ЗарплатаКадрыРасширенныйСинхронизацияДанных.ШаблонОбработчикаПослеОбработки
Процедура ПослеОбработкиПравилаРегистрации(Объект, Отказ, Параметры) Экспорт
	
	Если Параметры.Выгрузка Или Параметры.Получатели.Количество() = 0
		Или Объект.ДополнительныеСвойства.Свойство("ОпределениеПолучателейПриЗаписи")
		Или Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
		Возврат;
	КонецЕсли;
	
	Если Параметры.Получатели.Количество() > 0 Тогда
		СинхронизацияДанныхЗарплатаКадрыСервер.ВыполнитьПравилаРегистрацииПоДвижениямДокумента(Объект,
			Параметры.ПРО.ИмяПланаОбмена, Отказ, Параметры.Получатели);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
