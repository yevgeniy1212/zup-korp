
#Область СлужебныеПроцедурыИФункции

// Возвращает путь к каталогу, где хранятся электронные курсы.
//
// Параметры:
//   ПутьКОбщемуКаталогу - Строка - путь к общему каталога, где хранятся файлы электронного обучения.
//
// Возвращаемое значение:
//  Строка - Путь к каталогу.
//
Функция ПутьККаталогуЭлектронныхКурсов(Знач ПутьКОбщемуКаталогу) Экспорт
	
	#Если ВебКлиент ИЛИ МобильныйКлиент Тогда		
	ВызватьИсключение НСтр("ru = 'Использование общего каталога в веб-клиенте не допускается'");	
	#КонецЕсли	
	
	ПутьКОбщемуКаталогу = ЭлектронноеОбучениеСлужебныйКлиентСервер.ДобавитьКонечныйРазделительПути(ПутьКОбщемуКаталогу);
	ИмяОбластиДанных = "";	
		
	Возврат ЭлектронноеОбучениеСлужебныйКлиентСервер.ПривестиСлэши(
		ПутьКОбщемуКаталогу + ИмяОбластиДанных + ИзучениеЭлектронныхКурсовСлужебныйКлиентСервер.ИмяКаталогаЭлектронныхКурсов() + "\");
	
КонецФункции

// Открывает электронный курс для просмотра, редактирования или проверки.
//
Процедура ОткрытьЭлектронныйКурс(ЭлектронныйКурс,
							  Контекст = Неопределено,
							  Учащийся = Неопределено,
							  ВладелецФормы = Неопределено,
							  ЭлементСодержания = Неопределено,
							  РежимПроверки = Ложь,
							  РежимРедактирования = Ложь,							  							  
							  РежимОткрытияОкна = Неопределено,
							  Заголовок = Неопределено,
							  Окно = Неопределено) Экспорт
							  
	Если НЕ ЗначениеЗаполнено(ЭлектронныйКурс) Тогда
		ВызватьИсключение НСтр("ru = 'Электронный курс не задан'");
	КонецЕсли;							  
							  
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЭлектронныйКурс", ЭлектронныйКурс);
	ПараметрыФормы.Вставить("Контекст", Контекст);
	ПараметрыФормы.Вставить("ЭлементСодержания", ЭлементСодержания);
	ПараметрыФормы.Вставить("РежимПроверки", РежимПроверки);
	ПараметрыФормы.Вставить("РежимРедактирования", РежимРедактирования);
	ПараметрыФормы.Вставить("Учащийся", Учащийся);
	ПараметрыФормы.Вставить("Заголовок", Заголовок);
	
	ОткрытьФорму("ОбщаяФорма.ЭлектронныйКурс", ПараметрыФормы, ВладелецФормы,, Окно,,, РежимОткрытияОкна);
	
КонецПроцедуры

Процедура РедактироватьЭлектронныйКурс(Знач ЭлектронныйКурс) Экспорт
	
	Если ТипЗнч(ЭлектронныйКурс) = Тип("СправочникСсылка.ЭлектронныеКурсы") Тогда
		ПоказатьЗначение(, ЭлектронныйКурс);
	Иначе
		ВызватьИсключение НСтр("ru = 'Неверный тип электронного курса'");		
	КонецЕсли;
		
КонецПроцедуры

Процедура СоздатьЭлектронныйКурс(Знач Тип, ВладелецФормыКурса = Неопределено, КонтекстРедактирования = Неопределено) Экспорт	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ЗначенияЗаполнения", Новый Структура("Тип", Тип));
	ПараметрыФормы.Вставить("КонтекстРедактирования", КонтекстРедактирования);
	ОткрытьФорму("Справочник.ЭлектронныеКурсы.ФормаОбъекта", ПараметрыФормы, ВладелецФормыКурса);			
КонецПроцедуры

Процедура СкопироватьЭлектронныйКурс(Знач ЭлектронныйКурс, ВладелецФормыКурса = Неопределено, КонтекстРедактирования = Неопределено) Экспорт	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ЗначениеКопирования", ЭлектронныйКурс);
	ПараметрыФормы.Вставить("КонтекстРедактирования", КонтекстРедактирования);
	ОткрытьФорму("Справочник.ЭлектронныеКурсы.ФормаОбъекта", ПараметрыФормы, ВладелецФормыКурса);			
КонецПроцедуры

#КонецОбласти
